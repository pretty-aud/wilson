# Fix plan, decided 2026-09-04

Audrey answered 41 questions (plus two on sequencing) about how each known or
suspected problem should be handled. This file is the record of those answers
and the session plan that follows from them. It sits above
`MASTER_PLAN_S19_ONWARD.md` for ordering until it is exhausted; that file keeps
the standing rules and the older ledger.

Effort labels: **Quick** = an hour or two, folded into the nearest session.
**Half** = half a day. **Session** = a focused day. **Big** = several sessions
or a design first. **Hers** = needs Audrey's logins or hands.

---

## The sequence she chose

1. **Fix sessions first, on `feat/multi-user-v1`.** Quick items ride along
   with whichever session is nearest. Each feature ships with a scripted
   walkthrough she follows and reports on (answer 18).
2. **The UI overhaul branch starts after the fixes**, from the result
   (answer 41). Visual decisions taken here, such as the light-page field
   contrast and the look of the company-first sign-in, are executed there.
3. **Release last:** the `RELEASE_TESTING.md` pass, then merge PR #4 with a
   MERGE COMMIT (never squash), then tag 1.0.0. After everything, including
   the overhaul (bonus answer 1).

**Next session (bonus answer 2): S45, the setup link's second review pass,
its deploy, and the first walkthrough script (the pet reading lessons).**

---

## Session plan

Numbers continue from S44. Order is a proposal; she reorders as she likes.
"Walkthrough" means a written step-by-step script with what she should see,
delivered with the session, per answer 18.

| # | Session | Contents | Effort |
|---|---|---|---|
| S45 | Setup link + pet walkthrough | Second adversarial review of `df257d0` (answer 1/39); deploy `operator-workspaces` to staging and dev (0066 is already there; prod waits for 0064 + 0066); walkthrough for the setup link; walkthrough for the pet reading lessons, her first test (answer 19). | 1 session |
| S46 | Quick sweep | Merge the `maySuggest` fix branch `b051e20` (2); pet is cloud-only, say so on screen (5); password reset keeps signing out everywhere, say so on screen (12); roster load failure shows an error (17); single-instance lock on the desktop (30); remove O.T.T.E.R.'s dead Tools → Storage Location field (28b); "are you sure" before a dependency re-wire (7); audit line when a manager approves their own nomination (36). | 1 session |
| S47 | Timeline | Ghost links after a desktop delete and links dropped by the desktop→cloud migration (8); warn, naming the unfinished dependencies, then let the person continue (9, the unbuilt Phase 7); milestone trash + undo (38). | 1–2 sessions |
| S48 | Pet lifecycle | Refuse to save an older copy over a newer one, stale window gets a refresh notice (4); the death trio: corpse-forever on a fast close, Pet Mode off must pause hunger while closed, shared-computer adoption (6). | 1 session |
| S49 | O.T.T.E.R. | Settings switch to view the local library while signed in (3); change-request approval moves the five reference documents too (37). | 1 session |
| S50 | Company-first sign-in | Her design (34): the person names their company first, the system confirms it, then asks for username + password scoped to that company; usernames unique only within a company; account creation checks duplicates within the company only. ⚠️ Design flag: confirming "this company exists" before credentials lets an outsider enumerate company names; word or stage that confirmation carefully. The resolver's "two matches is a miss" rule goes away once scoped. The first-screen visuals land in the overhaul. | 1 session |
| S51 | Local server lock | Per-launch token minted at startup, carried by every desktop request, checked by middleware (21). Touches every fetch the R.A.B.B.I.T. renderer makes. | 1 session |
| S52 | Auth hardening | Server-side records of sign-in, sign-out and failed two-factor (23); idle timeout and a maximum session length (23). Admin two-factor stays optional (her choice). | 1–2 sessions |
| S53 | Money and teardown | Hide invoice activity from non-managers, deletion records stay visible (22, Half); sweep the company's avatars at teardown and count them in the certificate (24, Half). | 1 session |
| S54 | Upload reservation | Reserve quota when an upload starts, release on completion or expiry (32); the same record closes the partial-upload certification gap (TPN-CONT-017). | 1 session |
| S55 | Own-bucket previews | Batch signer for previews in a customer's S3 bucket and a longer signed-URL life so long videos play (33). **Needs a test target:** she creates an S3-compatible test bucket (her Backblaze account will do) before this session. | 1 session |
| S56 | Cloud milestones | Table, policies, suite, adapter methods, like scenes and levels got (26). | 1 session |
| S57 | D.O.G. cloud attachments | Re-home deck attachments in cloud mode (27). Seven catalogued traps in `MASTER_PLAN.md` §6 #31. | 1 session |
| S58 | mpv video | Pro-format playback and stills through mpv on the desktop, as Nostalgia TV does (15, 15b). ⚠️ Check the licence of the mpv build the same way `resources/ffmpeg/README.md` demands for ffmpeg; Nostalgia TV's mpv lessons apply (raise the child after every spawn, the staleness gate). | 1–2 sessions |
| S59 | Freeze banner | "Connection lost, reload" banner when calls hang after a network blip (10). Proper reconnect logic deferred. | 1 session |
| S60 | Agent for real | Agent Skills that actually gate actions, and the agent reaching R.A.B.B.I.T. (29). Design first. | Big: 2–3 sessions |
| S38 | Google Drive | Moved INTO the first release (35). Two sessions after Google's verification; **the paperwork in `OWED_AUDREY.md` §13 starts now**, because the wait is Google's. | 2 sessions + wait |
| S61 | Changelog | Rewrite `CHANGELOG.md` from the commit history since 30 July (40). Last before the release test. | 1 session |

Roughly twenty to twenty-four sessions before the overhaul begins, then the
overhaul, then the release. The walkthrough scripts add no sessions; each
ships with its feature.

---

## Hers to do, no code involved

- Rotate the `smoke_admin` password on wilson-dev, keep it an admin, update
  the `DEV_PROBE_PASSWORD` secret, re-run CI (20). `OWED_AUDREY.md` §0.
- Roll staging's legacy `service_role` key.
- Paste the invite and recovery email templates into dev AND prod (13).
- Enrol two-factor and grant herself operator on prod before launch (14).
- Reproduce the avatar failure once on the beta and send the exact error
  text shown; no dev work until then (11).
- Create an S3-compatible test bucket for S55 (33).
- Start Google's OAuth verification paperwork for Drive (35, §13).
- Paste PR #4's title and description (text in the session record).
- Follow each walkthrough as it arrives; the full `RELEASE_TESTING.md` pass
  at the end.

## Accepted as known limits (no work)

- Web sign-in token in browser storage; record as an accepted risk (25).
- Deleted cloud files hold quota for 30 days; the message explains (31).
- Admin two-factor enrolment stays optional (23).
- Light-page field contrast waits for the overhaul (16).

---

## Every answer, for the record

| # | Item | Decision | Effort |
|---|---|---|---|
| 1 | Setup-link button fails | Second review pass, then deploy the function; she tests | Session |
| 2 | "Suggest a change" dead end | Merge her fix branch `b051e20` | Quick |
| 3 | Desktop sign-in hides local courses | Settings switch to view the local library while signed in | Session |
| 4 | Pet overwritten across computers | Refuse to save an older copy over a newer one | Session |
| 5 | Pet needs internet in local mode | Cloud-only; drop the offline promise, say so | Quick |
| 6 | Pet death oddities | Fix all three together | Session |
| 7 | Re-wire can lose a link | "Are you sure" first (she accepted it does not fix the loss) | Quick |
| 8 | Ghost links; migration drops links | Fix both | Session |
| 9 | Status skips unfinished dependencies | Warn, naming them, then allow | Session |
| 10 | Freeze after a network blip | Reload banner | Session |
| 11 | Avatar does not save | She reproduces it and sends the error | Hers |
| 12 | Password reset signs out everywhere | Keep, say so on screen | Quick |
| 13 | Emails only on staging | Templates into dev and prod | Hers |
| 14 | Operator console on prod | Set up before launch | Hers |
| 15 | Pro video formats | mpv, playback and stills | 1–2 sessions |
| 16 | Field contrast below AA | Fix in the overhaul | Overhaul |
| 17 | Roster failure looks empty | Show an error | Quick |
| 18 | Testing untested features | Scripted walkthroughs she follows | With each session |
| 19 | Test first | The pet reading lessons | — |
| 20 | Test account password public | Rotate, keep admin | Hers |
| 21 | Local server has no lock | Per-launch token | Session |
| 22 | Invoice activity visible | Hide from non-managers | Half |
| 23 | Auth hardening | Log sign-ins; idle timeout + session cap; 2FA stays optional | 1–2 sessions |
| 24 | Avatars survive teardown | Sweep at teardown | Half |
| 25 | Web token in browser storage | Accept, record | — |
| 26 | Milestones desktop-only | Build cloud milestones | Session |
| 27 | D.O.G. cloud attachments | Build now | Session |
| 28 | O.T.T.E.R. Storage Location field | Remove it (the General-settings project folder stays) | Quick |
| 29 | Agent Skills and R.A.B.B.I.T. agent inert | Build them for real | Big |
| 30 | Two desktop copies | Single-instance lock | Quick |
| 31 | Trash holds quota 30 days | Keep | — |
| 32 | Concurrent uploads exceed quota | Reserve space at upload start | Session |
| 33 | Own-bucket previews and long video | Build now; she tests against a test bucket | Session |
| 34 | Same username, two companies | Company-first sign-in, usernames scoped per company | Session |
| 35 | Google Drive | Move into the first release | 2 sessions + wait |
| 36 | Manager approves own nomination | Allow, with an audit line | Quick |
| 37 | Approval skips reference documents | Move them too | Session |
| 38 | Milestone delete has no undo | Trash + undo | Session |
| 39 | Setup link second review | Yes (see 1) | Hours |
| 40 | Changelog stale | Claude rewrites it from the commits | Session |
| 41 | Fixes vs overhaul | Fixes first, overhaul after | — |
| B1 | Release timing | After everything | — |
| B2 | Next session | Setup link + pet walkthrough | — |
