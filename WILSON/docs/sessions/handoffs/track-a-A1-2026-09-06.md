# Hand-off — Track A, bundle A1 (2026-09-06)

Written by the Track A session that did bundle A1, for the session that does
bundle A2. Read `HANDOFF_PROTOCOL.md` first; this file follows its §4 order.

## 1. Where you are

- **Track A — product logic. Bundle A1 is DONE and NOT MERGED.**
- Branch `track-a-product`, pushed to `origin`. Head: the commit after `a6b5f3d`, which adds this file. Bundle commits, oldest first: `5dcff98` (R2 fix + suite 35 + deploy),
  `ce0d73e` (suite 28 + shim), `ff23725` (handbook, OUTSTANDING,
  walkthroughs), `a6b5f3d` (merge of `feat/multi-user-v1` `845e9e5`,
  protocol fixes only).
- **Not merged into `feat/multi-user-v1`:** the protocol says a bundle merges
  only after its two review rounds AND Audrey's walkthrough report. R1
  (`df257d0`) and R2 (`5dcff98`) are done; her reports on walkthroughs 01–05
  are not back. The next session merges A1 when she reports (§6 below), and
  starts A2 on the same branch regardless.

## 2. State, measured, not remembered

| What | Value | How measured |
|---|---|---|
| Track A migrations used | **none** — 0067, 0068, 0069 all still free; suites 71–73 all free | `ls supabase/migrations | tail`, `ls supabase/tests/rls | tail` (67 migration files 0000–0066; 70 suites) |
| dev (`eqjzmnvkrakroyqxfsvw`) | 66 rows in `schema_migrations`; 0058–0064 + 0066 present, **0065 absent**; `platform_audit` CHECK carries `workspace.invite_sent`; 4 workspaces; `inet_server_addr` `…9d59…` | `db query` through a throwaway `--workdir` link, 2026-09-06 |
| staging (`rzkirvkotslbovzbsdfh`) | same shape: 66 rows, 0058–0064 + 0066, 0065 absent, CHECK carries the value; 1 workspace; `…9d47…` | same recipe, same day |
| prod (`rqyriuyldhovirbuievt`) | **not re-measured this session** — the `db query` against prod was refused by the desktop app's permission classifier (a read; I did not work around it). Last measured 2026-09-04: at 0063, no `invite_sent` value. `functions list` against prod IS allowed: `operator-workspaces` **v9**, 2026-08-08 | `functions list --project-ref`, 2026-09-06 |
| `operator-workspaces` | **staging v9 → v10, dev v11 → v12**, both 2026-09-06 23:28 UTC; downloaded into scratch dirs and hashed: sha256 `a22eee07ae32ce1d…` on both, identical to `supabase/functions/operator-workspaces/index.ts` at `5dcff98` | `functions list`, `functions download --workdir <scratch>`, `sha256sum` |
| pgTAP | 70 suites. `tap-all` against **staging: 70/70** (through the scratch wrapper, see §5). Against **dev: 69/70** — the one red is `66_cloud_multi_gb.sql` probe 27, environment drift on dev, not code (§5, §6); two QUERY FAILEDs in the first dev run (31, 50) were the CLI's temp login role racing my concurrent single-suite runs on the same project, and both pass alone | `node scripts/tap-all.mjs` (dev) and the wrapper (staging) |
| vitest | **1712 / 71 files** (was 1706; +6 in `setupLink.test.js`) | `npx vitest run` on `5dcff98`'s tree; nothing after it touches `src/` |
| `deno check` | 4 errors on `operator-workspaces/index.ts` before and after the fix — the pre-existing baseline | `npx`-cached deno 1.46.3, see §5 |
| CI on the pushed head | **not run, by trigger**: `rls.yml` fires on push to `feat/**` or a PR into `main`/`feat/**`, so a track branch gets no CI — 0 runs for `a6b5f3d2f5325eef79c03318ef581ed8f32a7d8e`. The merged base `845e9e5` was green (RLS tests, 2026-09-06T23:34Z). The bundle's own run happens when it merges into `feat/multi-user-v1`, or if Audrey opens a PR `track-a-product` → `feat/multi-user-v1` | GitHub API, unauthenticated, full SHA (a 7-char SHA returns an empty list that looks like "never ran") |
| CLI link | `supabase/.temp/project-ref` and `linked-project.json` both wilson-dev, never moved | read after every env read |

## 3. Done and verified

1. **R2 adversarial review of `df257d0`, aimed at its corrections** — `5dcff98`.
   Two lenses (what does this break elsewhere; completeness). One real
   finding: the read-only `admin_contact` action called the now-throwing
   `loadFoundingAdmin()` bare, and `Deno.serve` has no error handler, so a
   transient lookup failure was a bare 500 without CORS headers → the console
   showed "Network error — check your connection." instead of
   "Could not look up this company's admin." Fixed by catching it the way
   `send_setup_link` does. Verified: `setupLink.test.js` grew a scoped
   `adminContactBlock()` (25 tests in the file); the four mutations the R1
   commit lists plus the new one each go red (1, 2, 1, 1, 2 failures) with
   the unmutated control green; `deno check` still 4 errors.
   Confirmed sound without change: both synthesized suffixes (`@wilson.invalid`
   from `create`, `@mail.petalstudios.co` from `admin-create-user`; nothing else
   mints addresses), the `already_suspended` guard above the mail, the shared
   `loadFoundingAdmin()`, WIL-7009 free. Recorded as limits in handbook §5.4:
   `admin_contact` spends the WRITE rate bucket; the Audit table never shows a
   row's code; nothing throttles a resend.
2. **Suite 35 scoped** — `5dcff98`. Probe 17 counts the two fixture
   workspaces; probe 19 inserts a second fixture operator (user_b) and counts
   both, so it still proves the cross-operator read rather than 0002's self
   arm. 28/28 on dev and staging (staging was have 5 want 2 / have 2 want 1
   before). Breakers: a wrong fixture id on either probe → have 1 want 2, red.
3. **Deployed** `operator-workspaces` to staging and dev, not prod — §2 has
   the versions and the hash. 🚨 The protocol (`ff7d5c9`) says spawned
   sessions are BLOCKED from `deploy` against staging. **This session was not
   blocked** — the deploy to `rzkirvkotslbovzbsdfh` ran and is hash-verified —
   so do not assume the wall; try, and fall back to the hand-off route only
   if refused.
4. **Suite 28 scoped and the hosted shim extended** — `ce0d73e`. Found by the
   definition of done ("tap-all clean against dev AND staging"): the first
   full staging run was 68/70. Probe 12 of `28_otter_progress.sql` counted the
   whole table as postgres and staging now holds one real study record
   (verified by query: 1 row, 0 for the fixture course, fixture course
   absent) — scoped to the fixture course, breaker red (DELETE removed →
   have 2 want 0). `67_member_full_time.sql` had NEVER run through
   `scripts/tap-hosted.py`, which lacked `col_type_is`, `col_not_null` and
   `col_default_is` — QUERY FAILED on every hosted run since the suite landed,
   never a counted failure. Added the three in the shim's style; 14/14 on both
   envs; a six-probe scratch suite proved each new function fails on a wrong
   type, a nullable column and a wrong default (3/3 red, 3/3 green).
5. **Five walkthroughs** in `docs/walkthroughs/` — `ff23725`:
   `01_pet_reads_lessons.md`, `02_setup_link.md`, `03_course_nominations.md`,
   `04_uploads_and_files.md`, `05_company_lifecycle.md`. Every backticked
   label (124 across the five) was grep-verified against `src/` and
   `supabase/templates/` by a script; each step names the browser and the
   moment, has an expect line, an "if instead" line and a report template.
6. **Docs** — `ff23725`: handbook §5.4 lists `admin_contact` and
   `send_setup_link` with their refusals and limits, Appendix B registers
   `WIL-7009`; `OUTSTANDING.md` lost the suite-35 entry and the S43b
   "not usable yet" note (now "deployed and testable"), and gained a session-log
   row. Comment markers re-counted: 5 → 5 in OUTSTANDING, 0 in the handbook.

## 4. In flight

Nothing. The working tree was clean at every commit; `package-lock.json` was
reverted after `npm install` and never committed.

## 5. Traps hit (the brief did not say these)

- **The worktree is cut from `main`, not `feat/multi-user-v1`** — `ff7d5c9`
  now says so. `git merge --ff-only feat/multi-user-v1` first, then rename.
- **`docs/fixes/` is gitignored** and exists only in the canonical checkout;
  read it by absolute path (now in the protocol too).
- **Deno is not on PATH.** S43b ran `npx deno`; the binary is cached at
  `%LOCALAPPDATA%\npm-cache\_npx\13e44e71d2d6eaea\node_modules\.bin\deno`
  (1.46.3). Prepend that directory to PATH and run `deno check` from `WILSON/`.
- **The desktop app's tool harness collapses `\\` to `\` inside a bash
  heredoc.** A Python replacement script written that way lost its escapes;
  it only worked because every mangled sequence happened to be one Python
  preserves. Write anything with backslashes with the Write tool.
- **Python subprocess output from vitest is UTF-8 (emoji in test names)**;
  `subprocess.run(..., encoding="utf-8", errors="replace")` or the reader
  thread dies on cp1252.
- **`scripts/tap-all.mjs` has no `--workdir`.** To run the WHOLE set against
  staging without moving the repo's link, copy it to a scratch dir and insert
  `'--workdir', WORKDIR` into its `db query` args (one sed); run it from
  `WILSON/` with the throwaway staging dir as argv[2]. Two full runs against
  two projects at once are fine but slow (30+ minutes each).
- **The prod `db query` was refused by the permission classifier** even
  through a throwaway workdir; `functions list --project-ref <prod>` was not.
- **Suite 28 decays the day someone studies on the beta; suite 67 never ran
  on the hosted shim.** Both fixed (`ce0d73e`). The general rule stands:
  every postgres-side `count(*)` in a suite must be scoped, and a suite that
  QUERY FAILs is not a red assertion and does not count against the total —
  read the per-suite lines, not only the summary.
- **`WIL-7009` was "verified free" but unregistered** in handbook Appendix B,
  and §5.4's action list did not know either new action. The four-place
  vocabulary test guards the ACTION list; the certificate-code registry is
  guarded by nobody but the reader.
- **Dev's `file_events_event_check` has drifted back to 0057's eight-value
  list** (it admits `upload_abandoned`) while 0058 is recorded as applied and
  every other 0058 artefact is intact — no purge function, no cron job, no
  function referencing `s3_multipart_uploads`, one vocabulary constraint, zero
  rows with the value. Cause unknown; not a 0057 replay; staging is correct.
  Suite 66 probe 27 is red on dev only. **The DDL to re-narrow it was refused
  by the desktop app's permission classifier** (a schema write against dev,
  even inside a transaction with 0058's own post-checks), so it is hers or a
  permitted session's: `docs/sessions/handoffs/track-a-A1-dev-renarrow-0058.sql`.
- **CI never runs on a track branch.** `rls.yml` triggers on push to `feat/**`
  and on PRs into `main`/`feat/**` only, so "CI green on the pushed head" is
  unmeasurable here until the bundle merges into `feat/multi-user-v1` (a push
  there deploys the beta, so that merge IS the CI run) or a PR is opened.
- **Two `tap-all` runs on the SAME project race the CLI's temp login role**
  ("Initialising login role…" rotates it per call): the loser's suite reports
  QUERY FAILED with a SASL auth error. One run per project at a time; a
  QUERY FAILED is not a red assertion and is not counted — read the per-suite
  lines, not the summary.

## 6. Waiting on Audrey

- **Walkthrough reports for 01–05** (`docs/walkthroughs/`). Order she chose:
  02 (setup link, on the beta's operator console) then 01 (the pet, on the
  desktop). A1 merges into `feat/multi-user-v1` only after those two report
  back clean; 03–05 can trail.
- **Resend policy** (walkthrough 02 step 11): a second setup link to the same
  company is allowed today; S43b's open question 6 is hers.
- **Dev repair, one command from `WILSON/` with the CLI linked to wilson-dev**
  (check `supabase/.temp/project-ref` first):
  `supabase db query --linked --file docs/sessions/handoffs/track-a-A1-dev-renarrow-0058.sql`,
  then `node scripts/tap-all.mjs 66` → 30/30. Until then dev's full run is
  69/70 for a reason that is not code.
- **Prod** gets 0064 → 0066 → the function deploy from the release session,
  in that order (wrong order = a send that reports success and writes no
  certificate). Not this track's.
- If any staging command is refused in HER spawned session (see §3 item 3):
  `supabase functions deploy operator-workspaces --project-ref rzkirvkotslbovzbsdfh`
  is the one A1 needed, and it has already run.

## 7. Next session's first three steps (bundle A2, session 1 — the timeline)

1. `git fetch origin && git checkout track-a-product` (the worktree is cut
   from `main`; the paths below do not exist until then), then the rest of
   `HANDOFF_PROTOCOL.md` §2: `npm install --ignore-scripts`,
   `git checkout -- WILSON/package-lock.json`, copy `.env.local`, `supabase
   link --project-ref eqjzmnvkrakroyqxfsvw --password ""` from `WILSON/`,
   confirm both `.temp` files say wilson-dev.
2. If Audrey's reports on walkthroughs 02 and 01 are back and clean: merge A1
   into `feat/multi-user-v1` with `git merge --no-ff` and push both (a push
   deploys the beta; 0066 and the function are already on staging). If not,
   leave it and say so in your hand-off.
3. Merge `feat/multi-user-v1` into `track-a-product` (`--no-ff`), re-run
   vitest, then start A2 session 1 exactly as `TRACK_A_product_logic_prompt.md`
   "Bundle A2 — Session 1" lists it: the re-wire confirm modal, the desktop
   ghost-link sweep with a replay control, the migration's dependency edges,
   Phase 7 per its brief (`docs/fixes/phase-7-dependency-status-warning/BRIEF.md`,
   canonical checkout). Read the five `OUTSTANDING.md` entries the bundle
   names before touching `TimelineView.jsx`.

## 8. Auto-memory

None, per `HANDOFF_PROTOCOL.md` §4 item 8 as amended by `ff7d5c9`: this file is
the memory. The controller session lifts what it wants into the main memory.
