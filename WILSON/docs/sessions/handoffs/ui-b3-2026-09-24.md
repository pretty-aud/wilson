# Hand-off — UI overhaul, Wave 3 lane B bundle B3 (R.A.B.B.I.T. Timeline), session one of two, 2026-09-24

Written by the first B3 session (Opus 5.5, max effort) at the protocol's
context trigger (§1: more than roughly 300 KB read), mid-bundle, as plan §5
budgets ("B3 Timeline, two sessions if the first hand-off trigger fires").
The next session is **B3b**. Nothing here asks Audrey anything (W14): every
open question is in §6.

---

## 1. Where you are

- Lane B, bundle B3: `views/TimelineView.jsx` (the Gantt, its summary band,
  toolbar, legend, minimap, `SettingsPanel`, `HelpModal`) and
  `components/EditHistoryDrawer.jsx`.
- Branch `ui/b3-rabbit-timeline`, cut from `origin/feat/ui-overhaul` at
  `4327368` (B2's CI line). Worktree `.claude/worktrees/elated-knuth-ee8a05`.
- The integration merge is **`cb4fecf`** (`origin/feat/ui-overhaul` at `ab45e03`,
  A3's O.T.T.E.R. commits, into this branch); this hand-off and the before
  screenshots are the commit on top of it. Both are pushed to
  `feat/ui-overhaul`.
- B3's commits so far, oldest first:

| commit | what |
|---|---|
| `2962c5c` | `writeGate.test.js`'s Timeline case reads each pane's own attributes (loosened first, mutant-proven) |
| `95aa902` | `scripts/timeline-state-shots.mjs`: 34 Timeline states photographed, for pixel proofs |
| `715bf56` | B2's CSS predicates MOVED into `rabbitCssGuards.js` (B2 asked; B2's 29 tests unchanged) |
| `97012ff` | **the state extraction**: both files, every branch a named variant; `views/rabbitTimeline.css` (stage 1, hex transcribed); `views/rabbitTimelineCss.test.js` |
| `86898c0` | the state-shots tool removes SMIL elements before each shot (it flickered) |
| `9fb85b7` | the minimap's arithmetic (`views/timelineMinimap.js`, 14 tests), the minimap probe (`scripts/timeline-minimap-count.mjs`), the render test as a PENDING contract |
| docs | this file and the before screenshots |

No restyle has landed. Every screen looks exactly as it did (§3).

## 2. State, measured

- **Vitest: 152 files / 3193 tests** after the merge, green. The baseline at the cut
  was 150 files / 3147 tests. With `.env.local` moved aside: the same, 152 / 3193, green.
- **CI** on the pushed head: read after the push; the follow-up commit
  "docs(b3): the hand-off's CI line" records it here.
- `inlineStateTernaries` (the lane's scanner): TimelineView **58 → 0**,
  EditHistoryDrawer **3 → 0**. The test's allowlist is empty.
- The walk after the extraction (`rabbit-timeline, rabbit-task-history,
  shell-nav, rabbit-settings, rabbit-help`): exit 0 at 1280x700 and 1440x900,
  nothing new (the before walk at 1024x700 on `rabbit-timeline`: exit 0).
- `ui-audit.mjs --css` before: 9 sheets. After: 10 (this lane's), 11 after
  the merge with A3's sheet.

### The minimap, before (Q22) — `scripts/timeline-minimap-count.mjs`, pre-extraction code

Nine phases is the fixture's five plus four made through the app's own
"Phase" form (`--add 4`, in-memory, a reload wipes them).

| size | phases | zoom | drawn | cut, silently | axis labels overprinting |
|---|---|---|---|---|---|
| 1440x900 | 5 | every minimap zoom and every detail zoom | 5 | 0 | 27 of 28 (default span), 4 of 24 (2 yr), 59 of 60 (5 yr), 0 at 6 mo / 1 yr / Fit |
| 1440x900 | 9 | every one of the ten | **5** | **4** | as above |
| 1280x700 | 5 | every one | 5 | 0 | 27 of 28, **22 of 24** (2 yr), 59 of 60 |
| 1280x700 | 9 | every one | **5** | **4** | as above |

The axis labels are 13px everywhere (on the scale; the defect is the
overprint). The minimap body is 124px at every zoom. Nothing on screen says
a phase is missing. The before shots of the minimap region at every zoom are
in the scratchpad of this session only; the full-page before shots are in
`docs/sessions/handoffs/img/b3-before-*` (below).

## 3. Done and verified

1. **`writeGate.test.js` loosened first** (`2962c5c`). The Timeline case used
   to slice from `<Pane` to the literal `"\n      />"`. It now scans each
   render's opening tag (braces, strings, template literals and comments
   tracked) and requires the last `canWrite` on the element, with no spread
   after it, to be `{canWrite}`, at EVERY render site. Proved on the real
   file (mutated, run, restored):

   | mutant | new | old |
   |---|---|---|
   | M1 prop deleted | FAIL | FAIL |
   | M2 `canWrite={true}` | FAIL | FAIL |
   | M3 a later `canWrite={true}` | FAIL | pass |
   | M4 prop inside a comment | FAIL | pass |
   | M5 a spread after the prop | FAIL | pass |
   | M6 a second, ungated render | FAIL | pass |
   | M7 the gate replaced by a literal | FAIL | FAIL |
   | M8 re-indented AND prop deleted | FAIL | pass |
   | L1–L3 legitimate re-indents and a reorder | pass | pass |

   The old slice never failed a re-indent: it ran on to the next six-space
   closer in the file and read whatever came after (M8). A CONTROL test keeps
   every shape as a snippet.

2. **The state extraction** (`97012ff`, done by a subagent on a written brief
   and verified here). Every hover / selected / active / disabled / dragging /
   drag-over / locked branch in both files is a `data-*` attribute (or a real
   `:disabled`) plus a rule in `views/rabbitTimeline.css`, values
   TRANSCRIBED (stage 1: the sheet still holds hex; `typeScale.test.js` has a
   one-file `STAGE_1_HEX` exemption and four judged 2px borders that stage 2
   deletes). `barTone()` is gone: its palette is the `.rb-tl-tone` rules keyed
   on `data-shape` (task / phase / subgroup), `data-status`, `data-life`
   (completed / upcoming / active) and `data-critical`; all 1,944 shape x
   status x date x critical cases resolve to the colours they had. The
   variant inventory is in the sheet's header.
   - **Pixel proof** (`timeline-state-shots.mjs`, 34 states, against the code
     before the extraction, both captured this session with the hardened
     tool): 1440x900 **all 34 identical**. 1280x700: 33 identical; the month
     zoom state differs by ONE colour level on eight anti-aliased pixels of
     the Today button's crosshair, in every run, and that button's code is
     unchanged by the extraction. Two further shots flicker by 2 pixels on a
     key-date diamond between runs of the SAME code (old and new alike).
   - **Dead branches it found** (each keeps its current look): the label
     row's `hover:bg-stone-800/50` never painted (an inline ground always
     won) and was dropped; the chart row's `default` cursor for kind `asset`
     is unreachable (no row builder makes that kind) and was dropped;
     `ZoomControls` has no caller; `hover:bg-stone-750` (not a Tailwind
     colour) never painted; the "Manage Task Templates" hover never painted
     (inline ground); `EditHistoryDrawer.jsx:174`'s `||` fallback is
     unreachable.
   - **Left inline on purpose** (not scanner hits): geometry; key-date
     markers' user colour (and the glow that appends `66` to it); the SVG
     `stroke` / `markerEnd` / `fill` ATTRIBUTES the dependency arrows choose
     by kind — 🚨 a stage-2 CSS `stroke` rule would override them, so the
     arrows' restyle must key on the kind; icon swaps with a fixed colour per
     branch (Lock / Unlock, the editor's header icon).
3. **B2's predicates moved** (`715bf56`): the nineteen B2 asked for, into
   `rabbitCssGuards.js`, parameterised by lane prefix with B2's as the
   default. `impossibleTones` / `toneSources` take their tone set with no
   default (B2's comes from app code that imports `ui/StatusDot.jsx`, and
   plain-node scripts import the guards module). B2's 29 tests and B1's 27
   are unchanged and green.
4. **The minimap's arithmetic** (`9fb85b7`, not wired): see §4.1.

## 4. In flight, and what B3b does next — in this order, one commit per surface

Every surface below starts from the extracted file. The sheet is
`views/rabbitTimeline.css` (classes `rb-tl-` and `rb-hist-`); its guards are
`views/rabbitTimelineCss.test.js`. Stage 2 (tokens instead of hex, then
delete `STAGE_1_HEX` and the four judged borders in `typeScale.test.js`)
happens surface by surface.

### 4.1 The minimap (Q22, the priority) — first

The arithmetic is written and tested in `views/timelineMinimap.js`; the DOM
contract is `views/timelineMinimapRender.pending.jsx`. Wire it:

1. `export` `OverviewPane` (a named export; the render test imports it).
2. Rows from `minimapLayout(overviewRows.length)`: `rowH` for every phase
   row (the old per-kind 22/11 goes: the minimap draws phases only —
   `buildOverviewRows` emits no task rows), the body's height from `bodyH`,
   and the pane's height = 24 (axis) + `bodyH` + 10 (scrollbar) + 2.
   `OverviewContainmentOverlay` already takes `rowLayouts`; feed it the same.
3. Mark the DOM as the contract reads it: the body `data-minimap-body`, each
   row `data-minimap-row={phaseId}` with inline `top` / `height`, each bar
   `data-minimap-bar`, an off-window phase's marker
   `data-minimap-edge="before|after"` (from `offWindow`, pinned to that edge,
   the phase's name beside the chevron when `named`), each name
   `data-minimap-name` with `text-caption`, each axis label
   `data-minimap-tick-label` with `text-caption` inside a positioned parent
   whose inline `left` is the tick's.
4. Names: inside the bar (or beside it when the bar is narrower than the
   name), Caption step, ink token, truncated, only while `named` (rows of
   18px or more); the hover card keeps every name.
5. The axis from `minimapTicks(span.start, span.days, dayPx)`: the month
   lines stay on every month; labels only where `label` is non-null.
6. The frame (review TL-18): the interactive frame keeps zIndex 5 (below the
   bars, so bar clicks win — review risk 5); draw its visible outline as a
   SEPARATE `pointer-events: none` layer above the bars, signal edges, so it
   reads over them. Delete the stale "2px orange border" comment. Verify all
   four gestures after: click to jump, drag frame, drag bar, Ctrl+wheel.
7. The zoom readout from `spanLabel` (it said "2 yr" for 2.35 years and
   "5 mo" after Fit on a 137-day project, below the slider's own minimum);
   the "6mo" / "5yr" ends from `spanLabel(min)` / `spanLabel(max)`; the snap
   marks from `snapLeft` with the thumb styled to a known width (14px) in the
   sheet — the old overlay's `left: 8, right: 8` was "the typical thumb
   half-width". Fit keeps its behaviour (C1); the readout now tells the truth
   when Fit goes under the minimum.
8. The hover card (TL-38) onto the kit's floating tokens; the milestone's
   colour as a dot, not the card's border. The two off-frame arrows onto the
   kit `IconButton` (they are orange fills with a white-ish glyph today, and
   have a `title` but no `aria-label`).
9. Rename `timelineMinimapRender.pending.jsx` to
   `timelineMinimapRender.test.jsx` in the same commit; it must pass (it
   counts drawn phases against the data at 1, 5, 6, 9, 12 and 30 phases, and
   with sub-phases; edge markers at a six-month window; names only at 18px
   rows; Caption step; no axis overprint at 183–1825 days on 1024 and 1400
   wide). Edit it only to fix a wrong assumption about the DOM, and say so.
10. Prove it in the running app: `node scripts/timeline-minimap-count.mjs 5257
    --add 0` and `--add 4` at 1440x900 and 1280x700 (and 1024x700), every
    row of the table above re-measured: drawn = phases, cut = 0, overprint 0,
    and `marked` for anything off-window. `--shots <dir>` photographs the
    minimap at each zoom for the hand-off.

### 4.2 The rest of the Gantt's chrome

- **Summary band → `Stat`** (its third caller after B1 and B2): eight tiles;
  one icon per metric (review TL-07: the calendar icon was used for three).
- **The legend, once**: the `.rb-tl-tone` variants (active / upcoming /
  completed, critical, phase / subgroup / task), key dates, the today line.
  Shared by the minimap and the Gantt — one component, one place.
- **Toolbar → the kit `Toolbar`**, and ONE segmented idiom (TL-01) for the
  zoom chips, the group-by set and the sort pair (the kit `Tabs`, underline,
  no fill; the group-by icons may take a visible label — a label is not a
  control). "TODAY" to sentence case (V1-14). "+ Task" as the kit primary
  `Button` (`signal-fill`, white — C6). Undo / redo onto `IconButton` with
  `aria-label`. C1: every control stays, same clicks.
- **Rows (TL-03)**: the label half and the chart half as one row — same
  height, same baseline, ONE hover across both (a shared hovered-row key; the
  label row's hover never painted before). Task names off the grey onto the
  Dense step in the sans at an ink that clears 4.5:1 (TL-02); dates and
  durations stay mono with `tabular-nums`.
- **Type (TL-04)**: T0 put most sizes on the scale; what is left is inline
  (`fontSize` in SVG, style objects). The walk reports 0 off-scale today.
- **Colour**: the palette onto the status tokens plus the phase / task
  distinction T2 measured (five channels: weight, border 1 vs 2px, an outline
  ring, height 24 vs 28px, palette). T2's walkthrough question 4 (the
  identical `font-semibold` branches) is answered by the extraction keeping
  two named rules (`rabbitTimeline.css`, phase and subgroup); leave the
  question with Audrey (§6).
- **Drag, zoom, pan, reparenting and dependency drawing stay exactly as they
  are (C1).** `ROW_PX_BY_ZOOM` is not touched (review TL-39 is "ask Audrey").

### 4.3 The overlays

- **`SettingsPanel`** onto the kit (it is a right slide-out over a
  `fixed inset-0` scrim): its two tabs onto `Tabs`, its two hand-rolled
  toggles onto `Switch`, buttons onto `Button`. "Manage Task Templates" keeps
  opening B2's `TaskTemplateManager` Dialog (restyle the opener only).
- **`HelpModal` onto `Dialog`** (720, reading). 🚨
  `views/bins/binsShortcuts.test.jsx` pins Help's text contrast on the
  modal's ground `#292524` — move it to the Dialog's ground in the same
  commit. The modal has no Escape today (the state-shots tool clicks its
  backdrop); the Dialog brings Escape (Q17, ruled).
- **`EditHistoryDrawer` onto `Drawer`** (TL-24): the kit's
  `--titlebar-offset`, never a hand-compensated padding; its three contrast
  failures (3.16, 3.65, 2.29:1 in the walk) go.
  `editHistoryFormat.js` / `editHistoryRevert.js` are logic, not ours;
  `ACTION_META`'s `color` fields are unused since the extraction (not ours to
  delete — record it).
- **Five `window.confirm` onto `Dialog` (W9)**: "Remove this dependency?"
  (inside the write gate — keep it there) and the task editor's four deletes
  (milestone, asset, phase, task).
- `TaskEditor` and `PhaseExtendModal` are overlays in this file that B3's
  plan line does not name; only their confirms are B3's. Record, do not
  restyle (§6).

### 4.4 Close the bundle

Walk (`rabbit-timeline,rabbit-task-history,shell-nav,rabbit-settings,
rabbit-help` at 1440x900, 1280x700; `rabbit-timeline` at 1024x700) and
`ui-audit.mjs --css` after; the minimap probe after; two adversarial review
rounds (`model: "opus"`; round two re-measures round one's corrections in the
running app, the minimap at every zoom above all); the hand-off; walkthrough
**42** (`docs/walkthroughs/42_ui_rabbit_timeline.md`; 39 and 40 are on the
branch, 41 is A3's if it lands first — take the next free number and say so),
copied to `C:\Users\Audrey\Desktop\WILSON walkthroughs\UI overhaul\`; after
screenshots at 1440x900 and 1280x700 into `docs/sessions/handoffs/img/`
(`b3-after-*`, beside this session's `b3-before-*`), the minimap included.

### What B4 and B5 inherit from B3 so far

Nothing new beyond B2's §4 table. `rabbitCssGuards.js` now holds B2's
predicates too (`stateLeaks`, `kitFights`, …): a lane imports them from
there, passing its own prefix.

## 5. Traps hit

1. 🚨 **THE BASH TOOL COLLAPSES `\\` TO `\`** (B1's and B2's trap, a third
   time: a mutant script's regex). Scripts through the Write tool.
2. 🚨 **THE DEPENDENCY ARROWS ANIMATE WITH SMIL, WHICH CSS CANNOT STOP.**
   `animation: none` leaves `<animateMotion>` running; pausing and rewinding
   each SVG still left one unchanged page cycling through three frames. The
   state-shots tool now removes the animation elements in the photographed
   page. Any pixel proof of the Timeline needs the same.
3. ⚠️ **A SUBAGENT'S EDITS HOT-RELOAD THE SHARED DEV SERVER.** A measurement
   taken while it edits reads half-edited code. The honest before numbers came
   from checking the pre-change files out into the working tree
   (`git checkout <sha> -- <files>`), measuring, and `git checkout HEAD --`
   back — that updates the index too, so check `git status` after.
4. ⚠️ **A NOT-YET-PASSING TEST IN `src/` BREAKS A PARALLEL SUITE RUN.** The
   render contract lives as `.pending.jsx` (vitest runs `*.test.*` only).
5. ⚠️ **FIT CAN SET THE MINIMAP SPAN UNDER THE SLIDER'S MINIMUM** (137 days
   against 183): the slider pins at its end and the old readout rounded to
   "5 mo". Fit's behaviour is C1's; the readout is what changes.
6. ⚠️ **THE OLD WRITEGATE SLICE NEVER FAILED A RE-INDENT — IT READ PAST IT.**
   A guard that passes a legitimate edit is not the same as a guard that
   checks the right thing; M8 is the case.
7. 📌 **`scanner line numbers are of comment-stripped code`**:
   `inlineStateTernaries` reports lines of `jsCode(src)`, not of the file.
   Locate a hit by its text.
8. 📌 **`.claude/launch.json` IS TRACKED** (B1 trap 2): the local
   `b3-worktree` entry (port 5257) was never staged.

## 6. Waiting on Audrey (W14: nothing blocks; carried to the end)

1. **The minimap grows to keep names.** Up to five phases it looks as it
   did; six keep 20px rows; seven to ten phases make it grow from 124px to
   at most 186px tall so each bar keeps its name; beyond ten, rows shrink to
   an 8px floor and the names move to the hover card; beyond 23 it grows 8px
   a phase so none is ever cut. Right trade (a taller minimap on a big
   project) or would she rather it never grew?
2. **The minimap names its phases** on the bars (new text, under Q22's
   "legible labels"). Keep?
3. **The minimap axis** labels months as "Oct", January as its year
   ("2027"), and the first label with its year when it fits. Right?
4. **The zoom readout** says "2.3 yr", "4.5 mo" (it rounded to "2 yr",
   "5 mo"). Fit still goes under the slider's six-month end on a short
   project. Keep Fit as it is?
5. **T2's question 4**, still open: the phase and sub-phase bars' labels are
   two named rules at the same weight (600).
6. **`TaskEditor` and `PhaseExtendModal`** (the task / phase / key-date form
   and the "extend the phase?" question) are not named in B3's plan line;
   their five confirms are converted (W9), their chrome is not. V2 or P1?
7. **Enter on a focused button toggles the companion** (B2's question 10,
   `App.jsx`, P1's): the Timeline's toolbar is all buttons.

## 7. Next session's first three steps (B3b)

1. In a fresh worktree cut from `main`:
   ```
   git fetch origin && git checkout -b ui/b3b-rabbit-timeline origin/feat/ui-overhaul
   npm install --ignore-scripts && git checkout -- package-lock.json
   ```
   Copy `.env.local` and add `VITE_DEV_AUTOLOGIN=tester` and
   `VITE_DEV_FIXTURES=1`. A launch entry on a free port with Vite's root
   positional (5241–5246, 5251–5257 are taken), proved with a marker. Expect
   **152 files / 3193 tests** or more.
2. Read this file, then `views/rabbitTimeline.css`'s header, then
   `views/timelineMinimap.js` and `views/timelineMinimapRender.pending.jsx`.
   Walk `rabbit-timeline` at 1440x900 and run
   `node scripts/timeline-minimap-count.mjs <port> --add 4` — its table
   should match §2's before table (nine phases, five drawn).
3. Wire the minimap (§4.1), rename the pending test, re-measure (§4.1 step
   10). Then §4.2 and §4.3, one commit per surface, then close (§4.4).

## 8. Auto-memory: none.
