# Hand-off — UI overhaul, Foundation 3 (F3): the kit requests — 2026-09-11

Session: Opus 5 (`claude-opus-5`), worktree `zen-pike-a3bb53`, branch
`ui/f3-kit-requests` cut from `origin/feat/ui-overhaul` at `661a3da`.
Plan: `docs/design/UI_OVERHAUL_PLAN.md` (§0 C1–C9, §2 Q5/Q17/W2/W9, §3 the
design system, §4 the kit contracts, §6 mechanics, §7 verification).
Protocol: `docs/sessions/HANDOFF_PROTOCOL.md` — §4 order below.
Previous hand-offs this continues: `ui-f1-2026-09-11.md`,
`ui-f2-2026-09-11.md`, and the four that filed the requests
(`ui-d1-`, `ui-d2-`, `ui-c1-`, `ui-c2-2026-09-11.md`).
Walkthrough for Audrey: `docs/walkthroughs/28_ui_kit_requests.md`.

## 1. Where you are

- Track: the UI overhaul. Bundle: **F3 — the kit requests** (the four
  finished lanes' filed requests against `src/ui/`, `src/index.css` and
  `src/ui/tokens.js`). Complete; two adversarial review rounds done.
- Branch `ui/f3-kit-requests`, pushed. Commits, oldest first:
  - `6e5df5c` feat(ui): the kit requests — `color-scheme`, Dialog focus,
    Escape forwarding, `tfoot`, the light-surface cluster, two tokens,
    `Button` Icon + loading, `Tabs` title, `.ui-field-row` / `-stack`.
  - `679ddea` fix(ui): the Dialog focus restore — found in the running app
    and not by a test — plus the StrictMode landing memory, one dead guard
    removed, and the before/after screenshots.
  - the integration merge of `origin/feat/ui-overhaul` (C3, Admin Terminal),
    the review-round commits and the docs commit follow — see
    `git log 661a3da..HEAD`.
- Integration into `feat/ui-overhaul`: **see the last lines of this file**
  ("Integrated"). If that line is missing, integrate per plan §6.3 before
  doing anything else.

## 2. State, measured

- vitest **REPLACE-COUNT**, green, and green again with `.env.local` moved
  aside (`mv .env.local .env.local.aside && CI=true npx vitest run`), which
  is how CI sees it. Baseline at `661a3da` was 135 files / 2539 tests; the
  merge of C3 brought 136 / 2656 before F3's own review-round tests.
- `npx vite build`: passes. The built sheet was grepped for the new rules —
  `color-scheme:dark` ×4 (two of them the page-level stand-ins that still
  exist in `dashboard.css` and `resources.css`), `color-scheme:light` ×1,
  `--color-danger-light:#7f1d1d`, and the `tfoot` rule with its
  `position:sticky;bottom:0`.
- CI: `rls.yml` runs on `ui/**` since `ae8f2da`, so the pushes to this
  branch run it —
  https://github.com/pretty-aud/wilson/actions?query=branch%3Aui%2Ff3-kit-requests
  (`gh` is not authenticated in these worktrees; read the conclusion there).
- No migrations, no Edge Functions, no Supabase link (UI only).
- Dev server for this worktree: **port 5263**, started from the worktree with
  `npx vite --port 5263 --strictPort` and opened with the Browser pane's
  `navigate` (NOT `preview_start` with a launch name — F1 trap 1, D2's §7).
  `.env.local` carries `VITE_DEV_AUTOLOGIN=tester` **and
  `VITE_DEV_FIXTURES=1`**; the fixtures are what made any of this checkable
  (see trap 3).
- Ports 5203, 5213, 5223 and 5233 were already taken by other sessions.

## 3. Done and verified

### 3.1 The request table — all twenty-six, with the decision

Every numbered request in `ui-d1` §7 (7), `ui-d2` K1–K8 (8), `ui-c1` §9 (5)
and `ui-c2` "Kit requests" (6) — twenty-six filed. The table has thirty rows
because D1's request 4 is five separate holes in five components and each is
tracked on its own.

**The line F3 drew:** a defect in a component that already exists gets fixed
here; a NEW component, or a fix whose adoption is several files inside a lane
that has already finished, is deferred with its reason.

| id | filer | what | blocking | decision |
|---|---|---|---|---|
| D1-1 | D1 | `Table` + `Th`/`Td`/`Row` for the migration panels | no | **already shipped** by F2; the adoption is D1b's |
| D1-2 | D1 | `Card` / `SectionTitle` / `Tabs` / `PageHeader` as re-exports in `SettingsChrome` | no | **not a kit change** — all four exist; the swap is D1's (plan §6.5) |
| D1-3 | D1 | `Button` needs an `Icon` prop (13 silent call sites) | yes | **done** |
| D1-4a | D1 | `Chip` and `Loading` take `surface="light"` and no CSS reads it | yes | **done** |
| D1-4b | D1 | `Spinner` and `Kbd` have no `surface` prop | yes | **done** |
| D1-4c | D1 | `Switch`'s light variant covers only its label | yes | **done** |
| D1-4d | D1 | `IconButton:disabled` on light resolves to `ink-3` | yes | **done** |
| D1-4e | D1 | a disabled light `Button` is identical to an enabled secondary | yes | **done**, as a form change |
| D1-5 | D1 | `GatedAction` dims with `opacity: 0.4` | no | **deferred** — `src/permissions/GatedAction.jsx` is not a kit file and not a kit-change adoption |
| D1-6 | D1 | `ProfileSection` needs a `surface` prop | yes (for C2) | **deferred** — a page component; re-filed as KR-1 and assigned to D1b. Both kit holes underneath it are fixed |
| D1-7 | D1 | `pageBars.js` 120/80 for Settings | no | **already shipped** by D1b (`bars(120, 80)`) |
| K1 | D2 | `.ui-btn:active { transform: scale(0.98) }` | no | **done**, at `--duration-state` (120ms), not the 100ms asked — see §3.3 |
| ~~K2~~ | D2 | `SectionTitle` | — | closed on integration |
| K3 | D2 | `Button` gains a loading state owning the label swap | no | **done** |
| K4 | D2 | `OtpInput` | no | **deferred** — a new component; its three call sites are D2's finished files |
| K5 | D2 | `.ui-dialog-error > span` should wrap | no | **done**, clamped at three lines |
| K6 | D2 | a light-surface danger ink in `@theme` | no | **done** — `--color-danger-light`; AuthShell now writes no hex |
| K7 | D2 | a light-surface destructive button treatment | no | **done**, as a fill |
| K8 | D2 | `color: ink-light` in `.ui-btn`'s light hover block | yes | **done** |
| C1-1 | C1 | `Toolbar` does not hold `.ui-tab` to 28px | yes | **done** |
| C1-2 | C1 | `Tabs` items cannot carry a `title` | no | **done**, and adopted on the Rate card. ⚠️ A `title` is a POINTER affordance: measured, a disabled `<button>` keeps `pointer-events: auto` and `elementFromPoint` returns it, so the tooltip does show — but a keyboard user still gets nothing from it, and what covers them is the `Banner` C1 put in the flow, which stays |
| C1-3 | C1 | `Table` has no footer slot | no | **done** + adopted on the rate card |
| C1-4 | C1 | `.ui-field + .ui-field` fires on a horizontal pair, and doubles a `gap` | yes | **done** — `.ui-field-row` and `.ui-field-stack` |
| C1-5 | C1 | `color-scheme` is set nowhere | yes | **done** — see §3.2 for what it does and does not fix |
| KR-1 | C2 | `ProfileSection` needs a `surface` prop | yes | **deferred** — same as D1-6 |
| KR-2 | C2 | no in-cell editor (`CellSelect`) | no | **deferred** — a new component; three page surfaces to adopt |
| KR-3 | C2 | `HoverActions` has no caller on the Dashboard | no | **no action** — it has callers elsewhere (`RateCardTable`), and C2's own conclusion was that the reveal is a rule, not a component |
| KR-4 | C2 | the kit sets `color-scheme` nowhere | yes | **done** — the same item as C1-5 |
| KR-5 | C2 | `Dialog` has no focus management | yes | **done** |
| KR-6 | C2 | `Input` should forward Escape to `onKeyDown` | yes | **done** |

Not in those four sections, recorded so nobody re-opens it: **F2 filed
`Menu`'s missing ARIA roles as a kit request and assigned it to A2** (the
first lane that needs a keyboard-navigable menu). F3 did not touch it —
adding `role="menu"` without the roving-focus model is the trap F1 named.

### 3.2 `color-scheme` — what it fixes, and the half nobody knew about

The rule, in `@layer base` beside the focus-ring scopes it copies:

```css
:root { color-scheme: dark; }
.wilson-light-scroll,
[data-surface="light"] { color-scheme: light; }
[data-surface="dark"] { color-scheme: dark; }
```

It inherits, so it is declared on the surface, not on the control, and the
dark-island rule is written last so a Dialog, Menu or Toast opening inside a
light page takes `dark` back — the same tie-by-source-order the focus ring
uses. The two page-level stand-ins (`.rs-page .ui-input`, `.dash-date`) are
left where they are: they now agree with the kit, and those files belong to
C1 and C2. `NotesView.test.jsx` pins the `dash-date` class, so the class must
stay on the element whatever happens to the rule.

**Proven in the running app** (R.A.B.B.I.T. → Salt Hours → Assets, which has
twelve `<input type="date">` with no local stand-ing): the field's own
calendar glyph goes from a dark block on a dark field to a legible light one.
`img/ui-f3-colorscheme-{before,after}-crop.png`, same cells, same page. Every
raw `<select>` in D.O.G. and O.T.T.E.R. now computes `color-scheme: dark`
too, through the page wrapper, without the kit touching them.

🚨 **THE PICKER PANEL IS A SEPARATE MECHANISM, AND IT DEPENDS ON THE
RUNTIME.** Measured, four ways:

| runtime | OS / `prefers-color-scheme` | CSS `color-scheme` | the panel |
|---|---|---|---|
| Electron 33 | light | `normal` (before F3) | **near-white** — the C9 breach |
| Electron 33 | light | `dark` (after F3) | **dark** |
| Electron 33 | dark | either | dark |
| headless Chromium (Playwright) | light | `dark` | **still near-white** |

So in the shipping runtime the CSS property IS the fix and no
`nativeTheme.themeSource` change is needed — which is the answer to the
question that change would have raised. But **headless Chromium ignores
`color-scheme` for the picker popup**, so a Playwright screenshot of the
panel is not evidence either way; do not "fix" a non-bug from one.
`img/ui-f3-panel-{before,after}-electron.png` are from a standalone Electron
probe (deleted after use), labelled as such in the walkthrough.

**The one place this property cannot satisfy C9 and the surface at once:**
the light pages' well is `well-light` (`#de9155`), a mid tone, so a UA in
`dark` mode paints a white glyph on it. The light branch is therefore `light`,
and a panel opened from a light page is the browser's near-white one.
**Measured 2026-09-11 and re-checked after the C3 merge: no `date`, `time`,
`month`, `week`, `datetime-local` or `color` input is mounted on any of the
four light pages** (home, settings, admin-terminal, help). If a lane moves a
date field onto one, that is a ruling for Audrey, not a default to inherit.

### 3.3 The other changes, each with what it replaced

| change | file | verified by |
|---|---|---|
| `Dialog` focus: initial focus (leaving an `autoFocus` child alone), a Tab trap on the topmost dialog only, focus restored to whatever had it | `Dialog.jsx`, `overlay.js` | 9 render tests; the six-step keyboard walk in the running app (§3.4) |
| `focusableWithin()` — the semantic filter, NOT a layout one | `overlay.js` | 5 tests including the control that shows a layout filter returns nothing under jsdom |
| Escape reverts **then** forwards to `onKeyDown` (W2, KR-6) | `Input.jsx`, `TextArea.jsx` | 4 tests, including one that the first Escape still does not reach a Dialog |
| `Table` takes `foot`, rendered into a real `<tfoot>`, sticky to the bottom | `Table.jsx`, `index.css` | 2 tests; adopted on `RateCardTable` |
| `Button` takes `Icon`/`icon` and `loading`/`loadingLabel` | `Button.jsx` | 6 tests; `loading` adopted on Team Members' rate dialog |
| `Tabs` items take `title` | `Tabs.jsx` | 1 test |
| `Spinner` and `Kbd` take `surface` | those files | 4 tests |
| `Table` `foot` adopted on the rate card — same eleven columns, same figures, the AVERAGE row now in a `<tfoot>`; **sticky only when there is one row** (`img/ui-f3-ratecard-tfoot-1440x900.png`) | `RateCardTable.jsx` | the page's own test asserts the rows are in `tfoot` and NOT in `tbody`, the element order is thead/tbody/tfoot, and an empty `summary` renders NO `<tfoot>` rather than an empty one |
| the light-ground cluster — Chip, Switch track, Loading skeleton, Spinner, Kbd, IconButton disabled, Button hover ink / danger fill / disabled form | `index.css` | 12 tests, each asserting the rule AND its cascade position |
| `--color-danger-light` and `--color-skeleton-light` | `index.css`, `tokens.js` | the tokens test: agreement, 6 new contrast pairs, 6 new failing controls |
| `.ui-btn:active` press + its reduced-motion removal | `index.css` | 1 test |
| `.ui-toolbar .ui-tab` at 28px | `index.css` | 2 tests |
| `.ui-field-row` / `.ui-field-stack` | `index.css`, `Field.jsx` | 3 tests; `.ui-field-row` adopted on Team Members' two rate pairs |
| the dialog footer error wraps, clamped at 3 lines | `index.css` | 1 test |

**Deviations and judgment calls, recorded:**

- **K1 asked for 100ms; the press is `--duration-state` (120ms).** §3.4 of the
  plan allows three durations and 100 is not one of them, for a two-percent
  movement nobody can time. One token to change if D2 disagrees.
- **The light destructive button is the ONE status colour drawn on
  `#f4a261`.** §3.2 draws none, and measured that is right for success (1.18),
  danger (1.09) and warning (1.04) — but an error message has to be readable
  and D2 had already shipped `#7f1d1d` locally after measuring three reds.
  It is in `@theme` now with its ground named and both raised surfaces pinned
  as failing. It is the walkthrough's one open question.
- **A disabled light Button takes the inert `well-light` fill and loses its
  hairline.** D1 asked for "a form change on this ground, not an ink change".
  `well-light` is §3.2's own vocabulary for a surface that is not a control.
- **A disabled light IconButton still differs only by the cursor and the dead
  hover.** A glyph has no fill or edge to change. Recorded rather than papered
  over with an opacity, which §3.1 bans outright.
- **`.ui-field-stack` has no caller yet.** It is the second half of C1's
  request 4 (a `gap` parent and the sibling margin ADD: C1 measured 32px down
  one column of a form and 16px down the other). C1's pages carry a local
  reset that this replaces the next time that lane opens them. Flagged rather
  than adopted, because adopting it means editing a finished lane's page.
- **`Button` now destructures `disabled` out of `...rest`** so `loading` can
  set it. `disabled={false}` and `disabled` behave exactly as before; the
  test asserts both.
- **`Table`'s `foot` is not sticky-safe with `table-layout: fixed` and a
  colSpan-only footer** — the rate card's footer declares no widths, which is
  correct because the head already declares the grid.
- **`DIALOG_WIDTHS` and every prop name are unchanged.** Nothing was renamed;
  five props were added (`foot`, `Icon`/`icon`, `loading`, `loadingLabel`,
  `surface` on two components, `title` on a Tabs item).

### 3.4 Verified in the running app, not from a stylesheet

Team Members with dev fixtures on, the rate dialog, keyboard only:

| step | result |
|---|---|
| focus on the row's rate button | `<button> "Edit rate card entry"` — outside |
| the dialog opens | `<input>` — **inside**, the `autoFocus` field, not Close |
| Tab through the form and the footer, then once more | `<button> "Close"` — **the wrap**: Close is FIRST in the focusable list, because it is in the header, so reaching it from the last footer button IS the trap firing |
| one Tab past that | `<input>` — the browser's own Tab, now safely inside |
| Shift+Tab from the dialog SURFACE | `<button> "Review"` — the leak round 1 found, closed |
| edit to 9999, Escape | value back to **42000**, dialog **still open** (W2) |
| Escape again | dialog closed, focus back on **"Edit rate card entry"** |

⚠️ The third row is the one worth reading twice. `Close` sits at the TOP of
the dialog and therefore at the START of the focusable list, so the press that
lands on it is the wrap, and the press after it is ordinary browser
behaviour. An earlier draft of this table credited the trap to the wrong one
of the two.

`img/ui-f3-dialog-{1-open,2-trapped,3-escape-reverted,4-closed}-1440x900.png`.

**The light Button, on App settings → Profile, measured from the page:**
`Remove` is `rgb(255,255,255)` on `rgb(127,29,29)`; `Change avatar` beside it
is `rgb(28,25,23)` on nothing with a `rgba(28,25,23,0.22)` hairline. Before
this session those two buttons were drawn identically.
`img/ui-f3-settings-profile-1440x900.png`.

**The light-surface rules were proven to RESOLVE, not merely to exist.** Chip,
Switch, Spinner, Kbd, the skeleton and the disabled IconButton have no light
caller yet — that is what D1 and C1 are waiting for, and a source-text
assertion only proves the rule is written. So each one's exact markup was
mounted into the live App settings page and `getComputedStyle` read back:

| | computed on the page |
|---|---|
| chip, rest | `rgb(28,25,23)`, hairline `rgba(28,25,23,0.22)` |
| chip, active | ink on `rgba(120,70,30,0.18)` with a full-strength ink edge |
| switch OFF | transparent track, `rgb(28,25,23)` inset ring, ink knob |
| switch ON | `rgb(28,25,23)` track, `rgb(244,162,97)` knob |
| spinner | arc `rgb(28,25,23)`, ring `rgba(28,25,23,0.22)` |
| Kbd | ink on `rgba(120,70,30,0.18)` |
| skeleton | `rgba(28,25,23,0.22)` |
| IconButton disabled | `rgb(28,25,23)` — was `ink-3` at 1.68:1 |

The probe was deleted; it is a cascade check, not shipped UI, and it is not a
substitute for the first real caller looking right.

### 3.5 Exports and callers (plan §6.7)

`src/ui/index.js` exports the same 33 components it did before; nothing was
added to or removed from the barrel, so `index.test.js`'s `AWAITING_A_CALLER`
list is untouched (`Panel`, `Drawer`, `Stat`, `Switch` — all still waiting on
their lanes). Every kit component was grepped for callers across `src/`
before and after; no component lost one. The three caller files touched were
touched for adoption only and each is named in §3.1.

### 3.6 The two adversarial review rounds

Both rounds ran as Opus workflows: a fan-out of reviewers, one per dimension,
then one verifier per finding whose job was to DISPROVE it. Only findings that
survived verification were fixed. Every reviewer had the running app with dev
fixtures, and several of the confirmations below are measurements from it
rather than readings of the source.

**Round 1 — six dimensions (Dialog focus, Escape forwarding, the CSS cascade,
components and callers, the triage, the tests): 60 agents, 24 findings
confirmed and 30 rejected.** Three HIGH, and the two that mattered are both
things F3 had asserted in a comment without checking:

1. **The focus trap leaked, backwards.** With focus on the dialog SURFACE —
   which `tabIndex={-1}` makes click-focusable and which the zero-focusable
   branch parks focus on deliberately — `active` was neither `first` nor
   `last`, and `node.contains(node)` is TRUE so it did not read as outside
   either. Both branches missed, nothing was prevented, and Shift+Tab walked
   out to a live control behind the backdrop. The verifier measured it on the
   settings page: focus landed on "Select root directory", with 493 of 496
   tabbables behind the backdrop. Nine tests were green because none of them
   ever put focus on the surface while the dialog had children. The trap now
   tests POSITION (`items.indexOf`), which folds the surface, `<body>`,
   anything behind the backdrop and any list-versus-tab-order disagreement
   into one expression.
2. **Every `<tfoot>` cell pinned at `bottom: 0`**, so a footer with two rows
   pins both to the same line and the second covers the first. The rate card
   summarises per currency and its own test mounts GBP and USD. Sticky is now
   `:only-child`; a multi-row footer scrolls, which is a real limit and is
   written down as one.
3. **`focusableWithin` disagreed with the browser's tab order** — an
   attribute-only filter keeps `display: none` and `visibility: hidden`
   elements, and a Tailwind `hidden` class is the common spelling here, so
   `last` could be an element the browser never reaches.

Plus: **"the state reads from the cursor" was false.** `:disabled { cursor:
not-allowed }` is in `@layer base` and every kit control sets `cursor: pointer`
in `@layer components`, a later layer, which wins whatever the selectors say.
Measured in Chromium: disabled `.ui-btn`, `.ui-iconbtn` and `.ui-chip` all
reported `pointer`. That sentence was half of F3's own justification for two
decisions about the light ground, so the three rules now repeat it.

And **six tests that could not fail**, each now able to: the `.ui-btn`
transition regex was matching `.ui-switch-knob` 267 lines away; the
reduced-motion slice started at `.auth-step`'s block 1,200 lines earlier and
ran to EOF; the IconButton assertion pinned source order, which is inert when
the rule wins on specificity; the Toolbar assertion made no specificity claim;
the light Chip count assertion checked that a selector existed, not what it
declared; and the "nothing focusable" Dialog test hand-built a div instead of
rendering the case that actually occurs (a busy dialog, whose Close is
disabled). Three changes had no test at all — the dialog error wrap, the rate
card's `<tfoot>`, and `loading` on the worked example.

REPLACE-ROUND-2

**What the rounds did NOT change, recorded so nobody re-opens it.** Thirty
round-1 findings were rejected on verification. The ones worth naming, because
they look like defects and are not:

- Escape forwarding does not break any existing caller. All seven kit
  Input/TextArea call sites that pass `onKeyDown` early-return on anything
  that is not Enter, so nothing fires twice and nothing closes that should
  not — the reviewer read every one.
- `Select` still has no Escape handling, so W2 is per-FIELD and not
  per-dialog. That is F2's recorded inconsistency, not something F3 changed,
  and adding it would be a behaviour change on eleven surfaces (C1).
- The `defaultPrevented` signal a caller can read after Escape is a constant,
  not an "it reverted" flag. True, and it is not offered as one.

### 3.7 Known limits, each measured and none of them a defect to chase

- **A disabled light IconButton and a disabled light Chip are legible but
  still look much like enabled ones.** A bare glyph and a bare label have no
  fill or edge to change, and the ground has one ink. They say "not allowed"
  on hover now, which they did not before. Closing the gap further is a
  design decision, not a bug fix, and it is the walkthrough's business.
- **A disabled light `Tabs` item has the same shape of problem**, and its rule
  is F2's: `.ui-tab:disabled` and `.ui-tab[data-surface="light"]` are both
  (0,2,0) and the light rule is written later, so a disabled light tab
  resolves to `ink-light` — legible, indistinguishable. Left alone because the
  fix is the same design decision, and no light page has a disabled tab today.
- **ProfileSection's Remove button on the DASHBOARD.** K7 makes a light
  `danger` Button a dark-red fill, and `.dash-profile-bridge` remaps six light
  tokens but not `--color-danger-light`, so on `paper` the button's white
  label is 10.02:1 (fine, and it finally reads as destructive, which is what
  C2 complained about) while its FILL against the page is 1.75:1 — a faint
  boundary. The real fix is KR-1, the `surface` prop that stops that panel
  claiming to be light on a dark page. **D1b owns it; this is the number.**
- **`.ui-field-stack` has no caller.** It is the second half of C1's request 4
  and C1's pages carry a local reset it replaces; adopting it means editing a
  finished lane's page, so it is flagged rather than taken.
- **`Kbd`'s and `Spinner`'s light variants have no caller either**, and Help —
  the obvious home for a key cap — renders no `Kbd` at all. Both are written
  down beside the rules rather than justified by a caller that does not exist.
- **C1's `.rc-total-row` is now dead code**: the kit's `<tfoot>` rule is
  (0,3,1) against its (0,2,0) and they declare the same values, so nothing
  moved. It can go when lane C next opens `resources.css`.
- **`binUi`'s own `TextInput` still returns before the caller.** That is Bins'
  local copy, which Audrey ruled stays until B6; the kit's contract and Bins'
  diverge until then, deliberately.
- **The first Escape in a field leaves focus on `<body>`**, because binUi's
  revert blurs and F3 did not change that. It is inside the backdrop in the
  sense that matters — the next Tab is caught by the trap and pulled back to
  the dialog's first control — but a keyboard user is momentarily on nothing.
  Changing it means changing `Input`'s blur, which is a W2 behaviour.
- **Dialog's focus effect runs once.** If a caller replaces the focused
  control while the dialog is open (a conditional field, a list that
  re-keys), focus falls to `<body>` and stays there until the next Tab, which
  the trap answers. A `focusin` recapture would close it and is more than
  Q17's "keep it minimal"; recorded rather than taken.

## 4. In flight

Nothing uncommitted at hand-off.

## 5. Traps hit

**1. 🚨 A GREEN SUITE PROVED NOTHING ABOUT THE FOCUS RESTORE, AND THE APP
FOUND IT IN ONE CLICK.** The first cut captured the return target inside the
effect. React applies a child's `autoFocus` during the COMMIT, which is before
any effect runs — so on every dialog that has an autoFocus field the captured
element was the dialog's own input, which is disconnected a moment later and
rejected by the `isConnected` guard. Focus fell to `<body>`. Nine tests
passed, because none of them opened a dialog with an `autoFocus` child from a
real trigger. **Capture it during render**; that is the only phase that still
sees the control the user was standing on. Plan §7's "every visual claim is
checked in the running app" covers keyboard claims too.

**2. 🚨 REACT 18 STRICTMODE MAKES INITIAL FOCUS A DEV/PROD DIVERGENCE.**
`src/main.jsx` wraps the app in `<React.StrictMode>`, so every effect runs
mount → unmount → mount in dev, and `autoFocus` does NOT fire on that second
mount (the DOM node is not recreated). With the return target fixed, the
second run found focus outside and took the first focusable control — a dev
build landing on Close where a production build lands on the field. The dialog
now remembers where it landed. **Any kit effect that moves focus, scrolls, or
registers something positional has to be idempotent across that cycle**, and
the way to test it is to render under a real `<StrictMode>`, not to describe
it in a comment.

**3. TESTER MODE CANNOT SEE MOST OF THE KIT; DEV FIXTURES CAN.** With
`VITE_DEV_AUTOLOGIN=tester` alone the Dashboard says "needs the cloud", there
are no rows, no dialogs and not one `<input type="date">` in the whole DOM —
so none of this was checkable. Adding **`VITE_DEV_FIXTURES=1`** (the
dev-fixtures session's switch) gives 8 members, 42 tasks, rate cards and
fifteen assets with date fields, and every claim in §3.4 came from it. Put
both lines in `.env.local`.

**4. THE BROWSER PANE'S VIEWPORT EMULATION BREAKS `ref` CLICKS.**
`resize_window` to 1440x900 sets the emulated viewport but the pane's
screenshot coordinate frame stayed 800x500, so every `computer` click
computed from a `ref` landed off-screen and silently did nothing. Drive with
Playwright instead (`@playwright/test` is a dev dependency and
`scripts/screenshot.mjs` shows the shape), and reset the pane with
`preset: "desktop"` when you are done. F1 trap 14 is the same trap from the
other side.

**5. A PLAYWRIGHT SCRIPT MUST LIVE INSIDE THE WORKTREE.** Node resolves
`@playwright/test` from the SCRIPT's directory, not the cwd, so a probe in the
scratchpad cannot import it. Write it under `WILSON/` and delete it after.

**6. THE NAV STRIP IS IN THE DOM BUT COVERED.** `.wilson-nav-item` buttons are
present and "visible" to Playwright while the strip is collapsed, but
`.wilson-chrome` intercepts the pointer, so a real click times out after 30
seconds with a confusing log. Drive them with
`page.evaluate(() => button.click())`.

**7. 🚨 HEADLESS CHROMIUM IGNORES `color-scheme` FOR THE DATE PICKER POPUP;
ELECTRON DOES NOT.** A Playwright screenshot showed a near-white panel with
`color-scheme: dark` computed on the input, which reads exactly like the bug
still being there. The four-way matrix is in §3.2. **The panel is not
evidence from a headless browser.** Electron 33 was the runtime that settled
it, driven with `webContents.capturePage()` and `sendInputEvent` — and the
Electron binary is NOT in a worktree installed with `--ignore-scripts`, so
run it from the canonical checkout's
`node_modules/electron/dist/electron.exe` (reading that path is fine;
junctioning `node_modules` is what plan §6.8 forbids).

**8. THE `Escape` FORWARD IS TWO HALVES AND ONLY ONE IS OBVIOUS.** `cancel()`
calls `stopPropagation()`, so forwarding the key to `onKeyDown` does NOT let
it reach a Dialog — W2's "the first press does not close" still holds, and a
test asserts it from the outside. The half that changed is that a caller's own
handler runs at all.

**9. A GUARD THAT CANNOT FIRE IS NOT A GUARD, AND THE BREAK-IT PASS IS WHAT
FINDS IT.** The focus restore shipped with a "never restore INTO this dialog"
condition. Reverting it left every test green, because the return target is
captured before the dialog's DOM exists and therefore can never be inside it.
It was removed. **Run the pass: revert each fix in turn and check the test
that covers it goes red.** On the first run it also found two tests that could
not fail — the topmost-Tab guard (both dialogs' handlers are on `document` and
the upper one runs LAST, so most arrangements are indistinguishable; the case
that separates them is a control in the MIDDLE of the topmost dialog) and
`.ui-toolbar .ui-tab`, which had no assertion at all.

**10. THE BASH TOOL'S HEREDOCS STILL CHOKE** on long CSS-and-regex blocks
(F2 trap 1). Write test files with the Write tool, or splice them in with a
Python script written by the Write tool. Also: the Write tool writes **LF**,
and every source file in this repo is **CRLF** — normalise afterwards.

**11. A `<select>` INSIDE `.ui-input` HAS NO `color-scheme` OF ITS OWN.** It
inherits from the page wrapper, which is why the rule is on the surface and
not on `.ui-input`. Putting it on the control would have missed every
hand-rolled Tailwind input in the tools, and those are the ones with no local
stand-in.

**12. 🚨 A COMMENT THAT EXPLAINS A DESIGN DECISION IS A CLAIM, AND THIS
SESSION SHIPPED THREE FALSE ONES.** "The state reads from the cursor" (the
cursor was `pointer`), "the IconButton fix is order, not specificity" (it is
specificity), and "Help is where the keys are documented" (Help renders no
`Kbd`). All three were written as justification for a real decision and none
was checked. **If a comment says why, measure the why.** The reviewers found
all three in minutes; none of them was visible from the diff.

**13. `@layer components` BEATS `@layer base` WHATEVER THE SELECTORS SAY, and
that cuts both ways.** F1 put the global `:disabled { cursor: not-allowed }`
in `base` so utilities could still win. The consequence nobody traced: every
kit control's own `cursor: pointer` — in `components` — beat it, so every
disabled kit button in the app has been showing a pointer since F1.
`.ui-input`, `.ui-tab`, `.ui-menu-item` and `.ui-switch` happened to repeat
the declaration; `.ui-btn`, `.ui-iconbtn` and `.ui-chip` did not.

**14. `position: sticky` PINS EACH CELL INDEPENDENTLY.** Two sticky rows at
`bottom: 0` are two rows in the same place, and the one you see is the last in
document order. There is no CSS that offsets a row by the heights of the rows
below it, and `nth-last-child` arithmetic only holds while every row is
exactly one row tall. A multi-row sticky footer is not a thing you can write.

**15. A FOCUS TRAP THAT COMPARES AGAINST `first` AND `last` HAS A THIRD CASE.**
The container itself. `node.contains(node)` is true, so "am I outside?" says no,
and it is neither end, so both branches miss and the browser's own Tab runs.
`indexOf` has no third case. The same shape catches focus on `<body>` and any
element the focusable list and the browser disagree about.

**16. `checkVisibility` IS THE ONLY VISIBILITY TEST THAT WORKS IN BOTH
PLACES**, and it is a feature test rather than a browser test: jsdom does not
implement it, so under the runner the semantic filter stands alone and the
tests stay meaningful. Drive it in a test by STUBBING it on the element —
`Object.defineProperty(el, 'checkVisibility', { value: () => false })` —
because jsdom cannot evaluate a style that would make it false. And leave
`opacityProperty` off: `opacity: 0` is exactly how `HoverActions` hides row
controls WITHOUT taking them out of the tab order, which is the dead end
Q17(b) exists to fix.

**17. A `:disabled` RULE THAT SETS THE COLOUR THE ELEMENT ALREADY HAS IS NOT A
RULE.** The light Chip's was exactly that, because the light block is written
after `.ui-chip:disabled` at equal specificity and already won. Before adding
a state rule on a one-ink surface, resolve what the state gets WITHOUT it.

**18. THE BREAK-IT PASS IS CHEAP AND IT FOUND SOMETHING EVERY TIME IT RAN.**
Revert each fix in turn; run only the test file that covers it; restore.
Three runs, three catches: two tests that could not fail, one guard that could
not fire, and one line (`checkVisibility`) that no test reached. It costs a
few minutes and it is the only thing that distinguishes a test from a
comment.

**19. A SOURCE-TEXT ASSERTION MUST MATCH THE DECLARATION, NOT THE SUBSTRING.**
`expect(block).not.toContain('bottom: 0')` fails on `border-bottom: 0`. And a
regex that walks from a selector with `[\s\S]*?` walks through closing
braces: read the BLOCK (`/\n  \.ui-btn \{([^}]*)\}/`) and assert inside it.

## 6. Waiting on Audrey

**Walkthrough:** `docs/walkthroughs/28_ui_kit_requests.md`. One question in
it, plus two things she will see change on App settings:

1. **The one question.** The filled dark-red destructive button
   (`--color-danger-light`, white at 10.02:1) is the only status colour drawn
   on `#f4a261` anywhere. Keep it, or go back to an outlined button that is
   indistinguishable from every other button? One rule either way.
2. **Three light danger buttons change shape today**: MfaSection's
   "Disable MFA" confirm (whose own comment says "the day the kit gains a
   light danger treatment, this call site starts rendering it with no edit"),
   ProfileSection's "Remove photo", and SessionSection's sign-out pair.
3. **A disabled light button** now sits in a recessed well with no edge.
4. The date picker in the packaged build at 125% and 150% scaling — the one
   claim that could not be checked here (dev fixtures are DEV-only, so the
   desktop build has no data to reach a date field with).

Carried, not F3's: walkthroughs 20–27 not yet reported; Bins waits for B6.

## 7. Next session's first three steps

```
git fetch origin
git checkout -b ui/<next> origin/feat/ui-overhaul
npm install --ignore-scripts && git checkout -- package-lock.json
```

Then copy `.env.local` from
`C:\Users\Audrey\Documents\My_Work\Dev_Work\wilson\WILSON\.env.local` into
`WILSON/` and add **both** `VITE_DEV_AUTOLOGIN=tester` and
`VITE_DEV_FIXTURES=1` (trap 3 — without the second one there is nothing to
look at), then start the server from the worktree with
`npx vite --port <free> --strictPort` and point the Browser pane at it with
`navigate`. 5203, 5213, 5223, 5233 and 5263 have all been used.

**What the kit can now do that it could not, so no lane hand-rolls it again:**
`Dialog` manages focus; `Input`/`TextArea` forward Escape after reverting;
`Table` takes `foot`; `Button` takes `Icon` and `loading`; `Tabs` items take
`title`; `Spinner` and `Kbd` take `surface`; Chip, Switch, Loading and
IconButton are correct on the light ground; `.ui-field-row` and
`.ui-field-stack` say which way a form stacks; `color-scheme` follows the
surface. **Five requests are still open and named in §3.1** — `OtpInput`,
`CellSelect`, `ProfileSection`'s `surface` prop, `GatedAction`'s opacity, and
`Menu`'s ARIA roles.

## 8. Auto-memory: none

The worktree's memory folder is empty and the main repo's is not this
session's to edit. This file is the memory; every lesson is in §5.
