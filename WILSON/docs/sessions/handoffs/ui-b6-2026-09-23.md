# Hand-off — UI overhaul, Wave 3 lane B, bundle B6 (Bins onto the kit), 2026-09-23

Model: Opus 5.5 (`claude-opus-5-5`). The brief named Opus 5 (`claude-opus-5`);
this session said so on line one and stopped; Audrey set 5.5 herself
(`/model claude-opus-5-5`, "5.5 set") and the controller recorded it. Every
line of work below is 5.5's.

---

## 1. Where you are

| | |
|---|---|
| Wave / bundle | 3, lane B, bundle **B6** — Bins onto the kit (plan §5; review `r-a-b-b-i-t-part-5.md`). Released by W13 without the track merges. |
| Branch | `ui/b6-bins`, cut from `origin/feat/ui-overhaul` at `fc2b523`; fast-forwarded to V1's `ee2fa7a` on the controller's GO |
| Last code commit | `1377aa7` (review round 2); then this file's docs commit and the audit-list line |
| Integrated? | INTEGRATION_LINE |
| Merged to `feat/multi-user-v1`? | No, and must not be (plan §6.4). |
| Ran in tandem with | A1 (D.O.G. part 1) and B1 (R.A.B.B.I.T. shell). B1's two `ProjectContextBar` slots landed at `0025e02` and are what let Q10 land here. |

B6's commits, in order (merges omitted):

```
a2ccf0a refactor(bins): the state extraction — every Bins state onto data attributes (B01)
d380631 feat(bins): binUi re-points at the kit — Bins renders on src/ui
e1fed1a style(bins): row hover, the ink ladder, the type — Bins onto the tokens (B02, B03, B04)
a5883e9 feat(bins): Q10 — the footer shortcut bar goes; its keys are in Help, its count in the tree
901ec47 feat(bins): W9 — the three native confirms on the Bins tab are kit Dialogs
4cb26b4 fix(bins): review round 1 — focus after a close, dimmed rows by ink, the type tag, W9's first focus
e194110 test(bins): review round 1 — the guards, hardened against the thirty mutations that got past them
1377aa7 fix(bins): review round 2 — round one's corrections, re-measured and finished
```

⚠️ **"Q10 last":** the brief asked for the footer bar's removal as the last
step. It landed as `a5883e9` right after the merge that brought B1's slots,
and W9 and the two review rounds followed it. Nothing after it touches the
bar, the slots or the Help card's contract; pushed history was not reordered.

---

## 2. State, measured

- **Vitest:** VITEST_LINE (baseline at GO: 141 files / 2915 tests). Each
  integration was run once more with `.env.local` moved aside: identical.
- **CI:** CI_FINAL
  Earlier integration, for the record: `87286c2` (round one) — run
  `35846525511` **red on the pgTAP job only, at its "Install Supabase CLI"
  step (infra: the setup-cli action with `version: latest`, the known flake
  of 2026-08-17)**; "Stop Supabase" failed as a consequence; vitest,
  Playwright auth and smoke passed on that SHA (the controller's read). Do
  not re-investigate it.
- **No migrations, no Edge Functions, no deploys** (UI only).
- **The walk** (`node scripts/ui-walk.mjs 5253 <W> <H> --only rabbit-bins,rabbit-scenes`),
  fixtures + tester mode, before (V1's `ee2fa7a`) and after (`1377aa7` merged
  with B1's and A1's work):

| Bins | contrast fails | C6 | borders ≥ 2px | clipped | faces (Geist / Geist Mono glyphs) |
|---|---|---|---|---|---|
| before, 1440x900 | 51 | 1 (the primary "Add", 3.35:1) | 2 | 9 | 739 / 1070 |
| after, 1440x900 | **0** | **0** | 1 (B1's `button.ui-tab`, the kit tab's 2px underline) | 9 | 832 / 848 |
| before, 1280x700 | 51 | 1 | 2 | 9 | 739 / 1088 |
| after, 1280x700 | **0** | **0** | 1 (the same tab) | 9 | 832 / 866 |

  "clipped" is the walk's count of text that ellipsises (file metadata on the
  tiles), not a pass/fail number (T2 §5 trap 5). Scenes (which renders four
  Bins components) measured 22 contrast failures after, all ScenesView's own
  text (B5's); it was pixel-identical to the before shot at rest after the
  swap (`d380631`).

- **`node scripts/ui-audit.mjs --css`**, before (V1's head, five sheets) and
  after (eight sheets: B1 added `rabbitShell.css`, A1 `dog.css`, and B6 adds
  `views/bins/bins.css` deliberately in the docs commit, with
  `typeScale.test.js`'s count moved 7 → 8). **`bins.css` contributes 0 hits
  on every row.**

```
                                                before (5 sheets)   after (8 sheets)
border >= 2px                                   18 in 4             22 in 6   (bins.css 0)
border-radius off the two                        5 in 1              5 in 1   (bins.css 0)
font-weight off the 400/600 axis                 1 in 1              1 in 1   (bins.css 0)
font: shorthand                                  0                   0
font-size off the scale                          3 in 1              3 in 1   (bins.css 0)
hex colour outside @theme                       28 in 1             28 in 1   (bins.css 0)
transition: all                                  0                   0
text-transform: uppercase off the Label step     0                   0
```

- **Bins files, before (`fc2b523`) and after, counted from source** (JSX,
  comments included — the hex matches left are in comments):

| pattern | before | after |
|---|---|---|
| hex colour | 34 | 2 (comments) |
| `rgba(` | 17 | 0 |
| `font-mono` | 36 | 20 (data only) |
| Tailwind stone / orange / red / green / amber utilities | 31 | 0 |
| `hover:bg-stone-*` | 10 | 0 |
| `window.confirm(` | 3 | 0 |
| raw `<button>` | 24 | 10 (see §3, "left hand-rolled") |
| raw `<select>` | 2 | 0 |
| a visual property chosen inline by a condition | 10+ | 0 (binsGuards' scanner, all spellings) |

---

## 3. Done and verified

| What | Commit | How verified |
|---|---|---|
| **State extraction** (plan §5's first commit): every hover / selected / active / current / drag-over / offline / disabled / tone branch in the Bins files onto `data-*` + `views/bins/bins.css`, values transcribed | `a2ccf0a` | Pixel diff: Bins and Scenes 0 px differ at 1440x900 against the before shots; walk census identical; `binsCss.test.js` + `binsState.test.jsx`; 7 mutants caught |
| **B01, the dead hover on every Bins button** | `a2ccf0a` | Headless probe: primary Add #ea580c → #c2410c on hover (its own darker fill, not the grey); Filters, sort, the unflagged chip, the Select mark → #44403c. Five more dead hovers of the same shape went onto the kit in `d380631` |
| **binUi re-points at the kit** — 13 local primitives deleted (−261 lines), re-exports + thin adapters, every contract difference in the table below | `d380631` | `binsDialogs.test.jsx`: ONE overlay stack (binUi's `overlayOpen` IS the kit's; the counters up and down; the inspector's Space stands down behind a Bins dialog); the backdrop click kept; mutants caught |
| `Switch` has its caller back — off `AWAITING_A_CALLER`; and the caller guard no longer counts binUi's own re-export as a caller (a Bins file importing the alias is) | `d380631`, `e194110` | `src/ui/index.test.js`, both directions; mutant "Toggle replaced by a checkbox" caught |
| **The live `data-bin-grid` template** (review risk 2) | `a2ccf0a`, `4cb26b4` | the attribute and its auto-fill template rendered; BinsView rendered with a stubbed 5-track grid, ArrowDown twice lands on the 6th tile; mutant "the width formula back" caught |
| **The `overlayOpen()` counters** (review risk 3) | `d380631`, `1377aa7` | one stack, the kit's; the Bins keys and the preview's Space stand down for a kit dialog or menu that is ON SCREEN (`visibleOverlayOpen`), not one left open on a hidden page |
| **C6 on the primary Add** (V1-02's KNOWN line for `rabbit-bins`) | `d380631` | walk C6 1 → 0; the KNOWN line deleted from `scripts/ui-walk.mjs` |
| **B02 row hover** — the kit's `--color-hover` on tree rows, list rows, frame tiles, both pick lists | `e1fed1a` | screenshots with a hovered row; the painted row states pinned by name |
| **B04 the inks** — `C` onto the ladder; the two failing inks are ink-3 (5.04:1). **The lift** (ink-3 and the signal fail on hover and selection grounds, 3.75–4.33:1: a painted row lifts them one step through custom properties that inline styles inherit). **The dim** (round 1: offline / locked / missing rows by ink, never opacity) | `e1fed1a`, `4cb26b4` | walk contrast 51 → 0; round two made files really offline in the browser: 4.66–5.04 at rest, 6.48–7.30 hovered or selected |
| **B03 / B27 the type** — the mono only on data; names in rows and tiles 600; metadata, hints, footnotes Caption 12; the list header the Label step; user-data chips keep their case | `e1fed1a`, `4cb26b4` | walk faces 1070 → 848 mono glyphs, 739 → 832 sans |
| C8 onto the kit: Banner, StatusBadge (the fourth private status ramp, B12), EmptyState, Field, IconButton, Select, Chip, ghost Button, Loading, the rename bar as a floating surface | `d380631`, `e1fed1a`, `4cb26b4` | screenshots; walk |
| Found by measuring: the kit Field's sibling margin fires sideways in a grid; the Day and Codec columns wrapped; the assign search was squeezed; the media-type tag failed over posters (2.49–3.96 → an opaque ground, 4.77–8.54); focus rings clipped by scroll boxes; the files pane had no ring at all | `e1fed1a`, `4cb26b4`, `1377aa7` | re-measured in the browser each time; the pane ring 11/11 on every side |
| **Q10** — the footer shortcut bar removed; every hint a row in Help → Shortcuts & Tips ("Bins: the keyboard"); the colour dots name their key; the project count in the tree's footer | `a5883e9` | `binsShortcuts.test.jsx`; `binsView.test.jsx` asserts every Help combo's EFFECT (a Help row with no effect fails); Help's inks measured on the modal's real ground |
| **W9** — the three `window.confirm` are kit Dialogs, opening on Cancel / Keep editing | `901ec47`, `4cb26b4` | BinsView rendered: the keys stand down behind the question, Escape closes only it, Remove removes; browser: six tiles, Delete, the question at 400px with the ring on Cancel |
| **C1 — where focus ends after a dialog closes** (rounds 1 and 2): as the Bins modal left it — on the page after a close from inside or once focus had entered the dialog; on the opener after an Escape when focus never entered | `4cb26b4`, `1377aa7` | jsdom (pointer, Enter, Escape, busy-then-code, autofocus + Escape, moved-inside + Escape, nested); browser: assign → Escape → Enter opens the rename bar |

### Two adversarial review rounds

- **Round one** (three reviewers, `model: opus`): behaviour and the kit
  contract (4 findings, none HIGH — focus return, W9's first focus, Relink's
  footer error, the keys behind the Help modal); the running app at both
  sizes (20 — the tag over posters, faded rows, the lift repainting the
  circled mark, clipped rings, density, Codec, capitals on user data, Help's
  retired orange…); the tests' strength (70 mutations, 30 got through —
  deleted colour rules, every other spelling of an inline state, a
  source-regex stand-down, a no-op lift…). Fixes `4cb26b4`, guards `e194110`
  (13 of the reviewer's 14 worst mutations then failed; the 14th — a dropped
  hook dependency — is harmless since the `overlayOpen()` stand-down).
- **Round two** (two reviewers): re-measured round one's corrections in the
  running app (five held, three partly, one made worse — Help's section title
  in the signal on the modal's #292524, 4.26:1) and attacked them in the code
  (the close's cause read at unmount, a hidden page's dialog killing the keys,
  thirteen states never rendered, a sign-off that was only a comment, Help
  keys checked by preventDefault alone…). Fixes and guards `1377aa7`; 12
  mutations applied across both sweeps, 12 caught.

### The binUi → kit mapping (every contract difference)

| binUi name | Kit | Kind | Difference a caller would notice |
|---|---|---|---|
| `Btn` | `Button` | re-export | `primary` / `danger` / `small` are Button's own aliases. **Unsized is md (36px, Body 14)**; Bins' was ~28px at 13px — toolbars pass `small`. Disabled is the ink-3 token, not `opacity-40`. `variant="ghost"` now reachable. |
| `IconBtn` | `IconButton` | adapter | **Unsized is sm (28px)**, the kit's default is md; a numeric `size` (3, 3.5) is sm. Ghost: no 1px line border at rest. Active is the signal tint + a 1px signal edge (was a full orange fill). Adds `aria-pressed`. |
| `Chip` | `Chip` | adapter | **A data colour becomes a 16% `color-mix` tint of it with a full-strength edge**, the ink unchanged — the kit would fill with the colour under 11px near-white (1.9–2.2:1 on the greens). Works for any colour string, a `var()` included (round 2). Label step, 28px, `aria-pressed`. B05 — Audrey's call, §6. |
| `Kbd` | `Kbd` | re-export | Label step mono, 11px (was 13px). |
| `Menu` | `Menu` | adapter (F1's) | `ColorDot:` items become the kit Menu's `leading` node. Paper-raised, 6px radius, the float shadow. |
| `Modal` | `Dialog` | adapter | **The backdrop click kept** (`dismissOnBackdrop`; the kit's default is off, Q17). **The four named widths**: 720 → reading; 820 and 860 → workbench; 520 → form; the old 640 default → form. **Focus management Bins never had** (initial focus, a Tab trap). 🚨 **Where focus ends after a close is the old modal's**, not the kit's: the adapter lets go of the kit's returned focus unless the dialog was closed by Escape AND focus never entered it (a child's autoFocus at open, or Tab / a click inside after) AND no other dialog is on screen. Title H2 sentence case; backdrop 60% (was 72%). |
| `Field` | `Field` | re-export | Same props. 🚨 `.ui-field + .ui-field` adds 16px top margin to a following field in a GRID too; Bins resets it with `.bn-field-grid` (kit request K-B6-1, §6). |
| `TextInput` | `Input` | adapter | **Default sm (28px).** Escape reverts and Enter commits through blur, as before; the caller's `onKeyDown` now also runs after an Escape (no Bins caller passes one). |
| `TextArea` | `TextArea` | re-export | Same props. |
| `Select` | `Select` | adapter | **Default sm (28px)** (the two pick dialogs' footers pass md beside md buttons). `onChange('')` → `null`, as before. |
| `EmptyState`, `Spinner`, `Loading`, `Banner`, `StatusBadge` | same | re-export | EmptyState's title is sentence case; Spinner takes a number still; Loading, Banner and StatusBadge are new callers from Bins. |
| `Toggle` | `Switch` | re-export | `checked` / `onChange` / `label` unchanged; the track is a real `<button role="switch">`, 36x20; the label text is still not a click target. |
| `overlayOpen` | `overlayOpen` | re-export | The kit's ONE stack. Bins itself asks **`visibleOverlayOpen()`** (new, in binUi): is a kit dialog or menu actually on screen (round 2: the web build keeps every page mounted under `display: none`, and back / forward does not close a dialog). |
| `C` | — | tokens | The ladder: every ink a property (`var(--bn-ink…, var(--color-ink…))`) so a painted row can LIFT the quiet ones and a dimmed row can DIM them all; `panel` → paper-raised, `line` / `faint` → rule, the oranges → the signal, green / red / amber → success / danger / warning. Nothing may append an alpha to a `C` value (a test holds it). |
| `MediaTag`, `ColorDot`, `ColorPicker`, `FlagMark` | — | local | Bins' data components. MediaTag's ground is opaque (its hue at 16% over the recessed paper) and always the sans; binMedia's two grey hues (the retired stone inks) take ink-2 / ink-3. |

**Left hand-rolled, on purpose (C8 has no kit counterpart):** the three
clear-in-field ✕ buttons; the list's sortable header cells and the marks cell
toggle (the list is a CSS grid, not the kit Table — §6); the inspector's
collapsible section headers; the two `ShotTakeChips` buttons (a thumbnail
strip); the offline link in the stats line; the search and rename fields stay
NATIVE inputs in the kit's `ui-input` class, because their Escape clears /
cancels where the kit Input's reverts (C1); the range slider and the native
checkboxes / radios (B29 is Audrey's; the kit has no checkbox).

---

## 4. In flight

Nothing. Every finding of both review rounds is fixed and guarded, or handed
back in §6 with its owner.

---

## 5. Traps hit

1. 🚨 **THE MAIN CHECKOUT IS ONE `cd` AWAY.** `C:/Users/Audrey/Documents/My_Work/Dev_Work/wilson/WILSON`
   is Audrey's main checkout (branch `feat/demo-2026-09-11`); this worktree is
   `…/WILSON/.claude/worktrees/<name>/WILSON`. Twice a command here was typed
   with the short path: once a read, once `git add … && git commit`. The add
   staged nothing and the `&&` chain stopped before the commit — verified by
   `git diff --cached` and the HEAD date. **Put the full worktree path in a
   variable at the top of every command.**
2. 🚨 **CRLF IN THE WORKING TREE, LF IN THE BLOBS.** A find-and-replace whose old
   string has `\n` matches nothing, and a mutant that "did not apply" looks like
   a mutant the test caught unless the script checks. Every mutant loop here
   checks `cmp` before believing a green.
3. 🚨 **THE BASH TOOL EATS BACKSLASHES IN A HEREDOC.** `\\'` came out as `'`
   (twice a broken string), and `/Clip \d\d/` landed as `/Clip dd/`, and a
   `${p}\\s*:` as `${p}\s*:` — which still matched, by luck. Write edit scripts
   that hold escapes with the file tool, never a heredoc, and grep for the
   escape after.
4. 🚨 **A MUTANT THAT BREAKS THE MODULE PROVES NOTHING** ("no tests" reads like
   a catch). And **a test whose data cannot tell the versions apart proves
   nothing either**: round two's first busy-close test opened the dialog busy,
   so its close button was disabled, the kit landed focus on the surface, and
   "entered" was true for both versions. Three such tests needed stronger data.
5. 🚨 **A GUARD THAT CHECKS PRESENCE, NOT WHAT PAINTS.** The first `sel()` asked
   that a selector appear in the sheet; the lift rule NAMES every painted state,
   so a deleted selection colour still "existed". Same shape: Q10's first test
   paired no key with its word; the Help keys were "handled" if preventDefault
   ran; a ratio "signed off" in a comment.
6. 🚨 **THE KIT DIALOG MOVES FOCUS; THE OLD BINS MODAL DID NOT.** Where focus ends
   after a close decides what the next Space and Enter do on a surface whose
   keys stand down for a focused button. It took both rounds to get the old
   outcome exactly (§3, `Modal`).
7. 🚨 **IN THE APP, ENTER NEVER ACTIVATES A FOCUSED BUTTON.** `App.jsx`'s window
   keydown (the pet toggle, ~1404–1417) calls `preventDefault()` on Enter for any
   non-text target. jsdom tests cannot see it (they do not mount App). Handed
   back (§6).
8. 🚨 **THE KIT FIELD'S SIBLING MARGIN FIRES SIDEWAYS** in a grid (§3, `Field`).
9. ⚠️ **INK-3 AND THE SIGNAL FAIL ON EVERY HOVER AND SELECTION GROUND** (3.75–4.33).
   The walk measures at rest. The kit Table lifts nothing in a hovered row.
10. ⚠️ **AN INLINE STYLE CANNOT BE BEATEN BY A CLASS, BUT IT INHERITS A CUSTOM
    PROPERTY** — the lift and the dim reach every inline-coloured descendant.
11. ⚠️ **A FOCUSABLE PANE'S OWN OUTLINE IS PAINTED OVER** by its scrolling and
    positioned children; draw the ring as an overlay above them.
12. ⚠️ **MEASURE ON THE REAL GROUND.** Round one set Help's section title in the
    signal after measuring it on the paper; the Help modal's ground is #292524.
13. 📌 **`import.meta.url` in a jsdom test**: `new URL('./', import.meta.url)`
    fails `fileURLToPath` ("must be of scheme file"); use
    `dirname(fileURLToPath(import.meta.url))`.
14. 📌 **`ui-audit.mjs --css` HAS A FIXED LIST** and `typeScale.test.js` pins its
    length; a new sheet is added deliberately (B1, A1 and B6 each did, 7 → 8).

---

## 6. Waiting on Audrey (nothing blocks; W14 — for the final pass)

1. **B05 — data-coloured chips and marks.** The filter chips (a media type, a
   mark) and the inspector's Select / Reject / Circled draw an active state as a
   16% tint of their colour with a full edge, not a solid fill (a solid fill
   under 11px near-white measured 1.9–3.15:1). Keep, or fill?
2. **The lift.** Orange data text (a tile's slate line, "1 shot") turns to the
   ink inside a hovered or selected row, because orange fails on those grounds.
   Acceptable?
3. **Chips holding your data** (a camera, a day, a scene, a tag) keep their case
   at 12px; every other chip is in the Label step's capitals. Keep?
4. **The project count** ("12 files in 5 bins") moved from the removed footer
   bar to the bottom of the bins list. Is that the right place?
5. **The files pane's focus ring** appears once the keyboard is in use (the
   arrow keys, a Shift-click): an orange frame around the files pane, which had
   none (the review's B20 — an invisible tab stop). Keep?
6. **Offline files** are the quiet grey now, not faded. Obviously offline enough?
7. **The Clear button's `Esc` key cap** stays in the selection bar (a control
   naming its own key). Keep?
8. **W9's questions open on Cancel**, so Enter backs out (Windows' default was
   OK). In the app Enter does nothing on any focused button anyway — see the
   App.jsx hand-back.
9. **Still open from the review** (structural or taste, not done): B29 the tile
   slider (discrete steps?); B33 the media-type hues (video's is #fb923c, a
   retired chrome orange used as data; the two greys moved to the ladder);
   B08 the toolbar's two rows and B25 the selection bar's wrap (the kit Toolbar
   cannot hold every control at 1280 without dropping one, C1); B16 / B17 the
   file menu's and the filter row's Hick's groupings.

### Handed back

- **Foundation / P1 — Enter is swallowed app-wide.** `App.jsx`'s pet-toggle
  keydown (~1404–1417) calls `preventDefault()` on Enter for any non-text
  target, so Enter never activates a focused button (a W9 Cancel, a dialog's ✕,
  the toolbar's Filters). Pre-B6; the pet itself is C5. Suggested: stand down
  for a `button, a, [role=button]` target, or while `overlayOpen()`.
- **Foundation — a visibility-aware overlay stack.** `overlayOpen()`,
  `modalDepth()` and `isTopModal()` count a dialog left open on a page the web
  build has hidden (every page stays mounted; back / forward does not close
  dialogs). Bins asks `visibleOverlayOpen()` locally; the kit could, or close
  page-local dialogs on navigation.
- **Kit request K-B6-1 — a grid container for Fields** (`.ui-field-grid`, or
  `.ui-field-row` with wrap). Bins carries it as `.bn-field-grid`.
- **Kit findings:** a hovered item's menu hint is ink-3 on the hover ground,
  3.98:1 (`src/index.css` ~1155 / 1161); an active Chip loses its tint on hover
  (`.ui-chip:hover` 0,3,0 beats `[data-active]` 0,2,0, ~979 / 983); the Menu
  has no reserved icon column (labels at four x positions when items mix icons,
  dots and neither) and does not flip upward (the 28-item file menu shows 10 at
  1280x700); the caller guard in `index.test.js` counts an IMPORT as a caller
  and misses default imports (`UsersSection.jsx`'s `import Menu from`).
- **B1 / B3 — the R.A.B.B.I.T. Help modal** is hand-built (TimelineView's
  `HelpModal`, opened by `Rabbit.jsx`): not on the kit stack, no Escape, its
  ground #292524 and its title / version line under 4.5:1; the Bins keys still
  act behind it (Ctrl+A selected all twelve files with Help open). Once it is
  the kit Dialog, `visibleOverlayOpen()` stands the Bins keys down on its own.
- **B4 — the file-table convergence.** `BinFileTable` is still a CSS grid of 16
  columns, not the kit `Table`. B6 restyled it in place.
- **B5 — what it inherits on Scenes.** `ShotTakeChips` keeps its `height` prop
  and every pixel height ScenesView passes (`Math.min(nestedThumbH, 22)`,
  `Math.min(rowH, 26)`, `20`); no size token was added. Through the four Bins
  components Scenes now has: the hover fill `--color-hover`, poster radius 3px,
  the primary ring from `bins.css`, the "+ takes" chip lifting on hover, the
  takes dialog and take picker as the kit Dialog (reading / workbench) with the
  old focus outcome, the role stripe a 2px inset edge (was a 3px border).
  Scenes' own 22 contrast failures are ScenesView's.
- **LOW:** the list's last sortable header ("Bytes") draws its ring past the
  scroller's right edge at 1280x700.

---

## 7. Next session's first three steps

For **V2** (after lanes A and B) and **P1**:

1. In a fresh worktree cut from `main`: `git fetch origin && git checkout -b ui/<bundle> origin/feat/ui-overhaul`,
   `npm install --ignore-scripts && git checkout -- package-lock.json`, copy
   `.env.local` and append `VITE_DEV_AUTOLOGIN=tester` and `VITE_DEV_FIXTURES=1`.
2. Walk Bins: `node scripts/ui-walk.mjs <port> 1440 900 --only rabbit-bins,rabbit-scenes -v`
   and at 1280x700; expect Bins' contrast and C6 at 0. The list view, the
   dialogs, hovered rows and offline files are NOT in the walk: open `/rabbit`,
   "Salt Hours", then the Bins tab with Playwright (fixtures have no offline
   file — round two rewrote the fixtures adapter in the browser to make some).
3. Read §6's hand-backs; the App.jsx Enter one affects every surface.

---

## 8. Auto-memory: none.

The worktree's memory is not the main repo's; this file is the memory.
