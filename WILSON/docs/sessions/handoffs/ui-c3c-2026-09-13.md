# Hand-off — UI overhaul C3c (Admin Terminal, the type and case pass), 2026-09-13

Model: Opus 5 (`claude-opus-5`). The chip required it and the session opened on
it, so unlike C3 and C3b there was no model stop.

**The caveat is gone.** C3b's §4 said the Admin Terminal "reads as the same
system" was a statement with a caveat on it, and named the caveat: 52 sites of
body copy at the 11px Label step and 14 uppercase runs in the two files Track
C and Track A own. That is done, and the sentence is now true without
qualification.

---

## 1. Where you are

| | |
|---|---|
| Lane / bundle | C, bundle **C3**, finished by **C3b**; this is its type-and-case follow-up, session **C3c**. Plan §5 Wave 3. |
| Branch | `ui/c3c-admin`, cut from `origin/feat/ui-overhaul` at `41d1bdd`, merged with it at `f819f55` |
| Last commit | `2ae9a21` |
| Integrated? | Yes — `feat/ui-overhaul` fast-forwarded to `2ae9a21`. |
| Merged to `feat/multi-user-v1`? | No, and must not be (plan §6.4). |

Commits, oldest first:

```
5d7f61a  feat(admin): the type and case pass — 52 sites of body copy off the 11px floor
0ea2dd3  test(admin): the case half of the pass had no guard at all, and a mutant proved it
1b0d4ae  fix(admin): raising the picker blurbs made their own titles two steps too small
95163a8  docs(walkthrough): part three — the type pass finished, and the caveat is gone
2bfa167  fix(admin): review round one — my own picker fix re-made AT-08 one level up
d873c90  test(admin): 34 of the review's mutants walked through these guards; now none does
90cf6e1  docs(admin): screenshots at the head, plus a seeded Change requests pair
1878e99  fix(admin): review round two — the card title was dimmer than the options inside it
597f31a  docs(handoff): round two, and the trap I wrote into this file and then hit
a95c102  Merge origin/feat/ui-overhaul into ui/c3c-admin
46c3af0  refactor(admin): two page-local kit copies retire, because the kit now carries them
4996ee9  docs(handoff): reconciled with the integration merge — §6 is empty now
2ae9a21  Merge origin/feat/ui-overhaul (F4 review round one) into ui/c3c-admin
```

Twelve of this session's own plus two integration merges. `feat/ui-overhaul`
moved TWICE while this session ran — F4 pushed its bundle, then pushed its
review round — so §6's kit requests were satisfied under me and the second
merge had to be taken after the first push attempt failed.

🚨 **Four of the nine are fixes to this session's own work, and not one came
from the test suite.** The suite was green before and after each. Two came from
opening the page and measuring it; two came from a reviewer measuring it. This
is the third session on this page to report exactly that, and it is now the
single most reliable fact about this surface: **the tests hold the fixes, they
do not find the defects.**

---

## 2. State, measured

- **vitest: 2815 passed / 139 files**, `CI=true npx vitest run`, AFTER the
  integration merge. This session's own head was 2740 / 137; F4 brings 75 more
  and two files. C3b closed at 2720 / 137. The 20 this session adds are all in
  `adminTerminalCss.test.js`, which goes 66 (C3) → 71 (C3b) → **91** here.
- **Run WITHOUT `.env.local`**: **identical, green.** Measured at 2738 / 137
  by a reviewer before the last two tests landed; Verified by a
  reviewer with `mv .env.local .env.local.aside && CI=true npx vitest run; mv
  .env.local.aside .env.local`, file restored and confirmed at 696 bytes.
  Nothing in the suite reads it.
- **`npx vite build`**: clean, three times. Only the two pre-existing warnings — the
  `sentry.js` dynamic-vs-static import, and the >2000 kB chunk.
- **CI status: still not retrievable, and now I can say exactly why it matters.**
  `gh` is on PATH (`C:\Program Files\GitHub CLI\gh`) but not authenticated, and
  neither `GH_TOKEN` nor `GITHUB_TOKEN` is set. Three sessions have owed this.
  **What is new: `.github/workflows/rls.yml` triggers on `ui/**`, and it does
  contain a Vitest job** (`npm test` → `vitest run`) alongside pgTAP and a
  Playwright `e2e-auth` job — so a CI status on this branch is meaningful, not
  just an RLS run. It also runs `npm ci` with no `.env.local`, which is exactly
  the environment verified green above. Runs are at
  `https://github.com/pretty-aud/wilson/actions?query=branch%3Aui%2Fc3c-admin`.
  **Ask Audrey to read it, or to run `gh auth login` in this worktree once.**
- **Migrations / Edge Functions / pgTAP:** none. UI-only; nothing applied to
  dev, staging or prod, no function deployed.
- **Dev server:** a `c3c-worktree` entry in the worktree's own (untracked)
  `.claude/launch.json`, port 5222. See §5 trap 1 — the `wilson-dev` entry
  already in that file serves the WRONG CHECKOUT and the app boots anyway.

  ```json
  {
    "name": "c3c-worktree",
    "runtimeExecutable": "npx",
    "runtimeArgs": [
      "vite",
      "<ABSOLUTE PATH TO YOUR WORKTREE'S WILSON DIRECTORY>",
      "--port", "5222", "--strictPort"
    ],
    "port": 5222
  }
  ```

  Vite takes the root **positionally**; `--root` is an unknown option and exits
  1. Prove it before trusting anything:
  `curl -s http://localhost:5222/src/ui/Table.jsx | head -c 40` must return a
  module, not `<!doctype html>`.

---

## 3. Done and verified

### `5d7f61a` — the pass

**49 hand-written sizes onto named steps.** 47 `text-[11px]` plus **two
`text-[12px]` the chip did not count** — both in `ChangeRequestsSection`, and
both had to go for the `text-[Npx]` scan to reach zero honestly. By role:

| Step | n | What |
|---|---|---|
| Dense 13 | 31 | warnings, plan explanations, picker prose, the change-request summary, status lines |
| H3 14 | 7 | five Storage card titles, the request row title, the confirm prompt |
| Label 11 | 7 | five S3 field labels, a status badge, an eyebrow |
| Caption 12 | 2 | a timestamp line, a required-field hint |

**`.at-prose`, the other five sites, in one line.** C3b introduced that class
holding the CURRENT size deliberately, with a comment saying the next session
raises it — so five strings moved with one change. Its leading now comes from
the step too. Verified in the app: an `.at-prose` span and a `text-dense` span
render **identically — 13px / 400 / 18.85px / normal**.

**14 uppercase runs → 7**, and the seven are every place the Label role
genuinely applies (Q2). This is precedent, not judgement: `.at-group-label` in
the converted siblings is already 11px / 600 / 0.06em / uppercase, so the five
field labels, the status badge and the eyebrow match what C3b shipped.

**All 14 `tracking-wider` gone.** It is 0.05em; `--text-label--letter-spacing`
is 0.06em. Every one was both wrong AND redundant. **All 17 `font-bold` gone**
— 700 against a 400/600 axis, and on the Label and H3 steps the theme already
emits 600, so each was overriding the scale to *leave* it.

**15 `leading-relaxed` gone.** The step carries its own leading, and
`HelpPage.jsx` — the reference implementation of the scale — uses no `leading-*`
at all.

**12 icons onto §3.3's three sizes.** Eleven `w-3 h-3` and one `w-2.5 h-2.5`
really did render at 12 and 10px: the utility is in `@layer utilities` and
`.ui-btn[data-size="sm"] > svg` is in `@layer components`, declared earlier.
Four sat inside kit buttons and had the override **deleted** rather than the
kit's number restated — which is what `Button` itself does (`<Glyph
aria-hidden="true" />`, no className). Measured after: every icon 14px.

**The last two hand-written buttons on the surface** are `ui-btn` ghosts. A
type pass alone would have left a 13px borderless button beside a 28px kit
Continue. Measured in the confirm panel: both 28px, both 13px/600/sentence
case, both 10px padding, differing only in ink — `ghost` is `var(--color-ink-2)`
and `INK_2` is `THEME['color-ink-2']`, the same value.

### `0ea2dd3`, `d873c90`, `1878e99` — the guards

`adminTerminalCss.test.js` 71 → **91 tests**. The `text-[Npx]` scan reached
zero exactly as C3b's own comment predicted, so **the floor became the scale**.
The full story is in §8, because most of it is review output.

### `1b0d4ae`, `2bfa167`, `1878e99` — three defects this session created

Both in §5; they are the session's real lessons.

### `95163a8`, `90cf6e1` — docs

`docs/walkthroughs/27_ui_admin_terminal.md` has a **part three**, in Audrey's
terms. Screenshots at §2's paths.

### Exports and callers, enumerated

Both files export one default component. `AdminTerminalPage.jsx` is the only
consumer of either (`:158`, `:161`). `.at-prose` and `.at-picker-label` are
named **nowhere in `src/` but `StorageSection.jsx`** — so the picker change
touches exactly the four rendered pickers. Three other test files read
`StorageSection.jsx` as text (`src/lib/workspaceRootWiring.test.js`,
`src/lib/storageRoot.test.js`, `.../storagePresignBoundary.test.js`); all green.

---

## 4. In flight

Nothing uncommitted; the tree is clean apart from the app's untracked
`.claude/` and the gitignored `.env.local`.

**Deliberately not done, with the reason:**

- **AT-20, the S3 card's three-block regroup.** Moves JSX; Track C owns the
  file and is 37 commits ahead. Unchanged from C3b.
- **`ChangeRequestsSection.jsx:333` reads a key that does not exist.**
  `style={{ color: st.color }}` against a `STATUS_TONE` whose entries are
  `{ tone, label }` — it evaluates to `{ color: undefined }` and the status
  badge inherits `--color-ink`, so the one element naming the status has no
  tone. Found by review. 🚨 **I did not delete it, and the reason is the merge:**
  Track A is 63 commits ahead on this file, and if their tree adds `color` to
  that map, deleting the attribute here would silently drop their colour when
  the branches meet. A conflict on that line is worth more than a tidy diff.
  **Track A should decide.**
- **`.at-dept-remove svg { width: 10px }`** in `adminTerminal.css:606` — a live
  10px icon against §3.3's three sizes. It is a 10px glyph in a 16px box inside
  a 20px `.ui-badge`, which only exists because the kit has no badge below its
  default and no icon button below 28px (§6). The icon guard now **names it as
  an exemption** rather than being silently unable to see it. Fixing it needs
  the kit request, and it is `CompanySection`'s element.
- **The S3 card's Prefix label wraps and misaligns its input by 15px.**
  Measured: "Prefix (optional folder inside the bucket)" needs **280px at the
  old 0.05em tracking and 285px at the new**, against a **224px** column — so
  it wrapped before this session and wraps now, and the 15px offset is
  pre-existing, not caused by the pass. Straightening it moves the label, which
  is AT-20's blocked change on the same card.
- **Eleven hand-rolled alert strips** beside the kit's `.ui-banner`, which
  already supplies the tone fill, `text-dense` and a 16px tone-coloured icon.
  In scope by the same reasoning that made the Cancel buttons `ui-btn`, but a
  bigger change than a type pass and it restructures markup. Recorded as C8 debt.
- **`darkBtnClass`** (`StorageSection.jsx:86`) is a module constant holding the
  string `'ui-btn'` with one caller. Harmless; deleting it is a JS change.

---

## 5. Traps hit

1. **🚨 THE `wilson-dev` LAUNCH ENTRY STILL SERVES THE WRONG CHECKOUT.** C3b's
   trap 1, unchanged and still the first thing that would ruin a session. Added
   the `c3c-worktree` entry (§2) before doing anything, and proved it with
   `curl /src/ui/Table.jsx`. Also probed for my own edit —
   `curl .../StorageSection.jsx | grep -c 'text-h3 mb-2'` — which is stronger,
   because it proves the server is serving *this commit*, not just this worktree.

2. **🚨 FIXING A HIERARCHY INVERSION BY MAKING A COLLISION.** Raising
   `.at-prose` to 13px left `.at-picker-label` at 11px, so four picker titles
   were smaller than the sentences they title. I raised the label to H3 — and
   at 14px/600 it became *character-for-character the card title above it*,
   which is AT-08's "the panel has no top" re-made one level up. **Both states
   passed every test.** The answer was the step between: Dense at 600, so the
   option is told from its blurb by weight and from its card by size.
   **When you move one end of a pair, measure the WHOLE stack, not the pair.**

3. **🚨 A GUARD THAT HOLDS YOUR WORK CAN BLOCK THE FIX FOR YOUR OWN DEFECT.**
   The two sentence-case parentheticals inside a field label inherit the Label
   token's 0.06em. The one utility that cancels it is `tracking-normal` — and
   this session's own new "no `tracking-*`" guard failed on it. The guard
   exempts `tracking-normal` by name now. **Every blanket ban needs its
   escape hatch named at the same time, or the next person writes the wrong
   thing to keep the suite green.**

4. **🚨 A MUTANT THAT MISSES ITS TARGET PROVES NOTHING, AND READS AS PROOF.**
   A `.at-prose` mutant replaced the FIRST `font-size: var(--text-dense)` in a
   sheet that has ten of them, so it edited a different rule and the guard
   reported green. I nearly recorded "the guard is inert". Re-anchored to
   `.at-prose` itself it fails correctly. **Before concluding a guard is inert,
   prove the mutation landed where the guard is looking.** This is C3b's trap 6
   with the count replaced by a test result.

5. **🚨 A MUTATION HARNESS NEEDS ITS OWN POPULATED-CHECK.** My replay harness
   passed a stripped environment to `subprocess`, `npx` never started, every run
   produced no output, and "no failures found" read as GREEN — **for all 34
   mutants at once**, which is the only reason it was obvious. It asserts
   `"Tests "` appears in the output now. A test harness is a derived list too
   (C3b trap 8).

6. **A HEREDOC MANGLED A PYTHON SCRIPT THREE TIMES**, exactly as C3b's trap 5
   says it will: once silently dropping `\n` inside a string (SyntaxError), once
   emitting `SyntaxWarning: invalid escape sequence` on `\s`, once producing an
   unterminated literal. **Write the script with the Write tool and run it by
   path.** Every heredoc I used had to be redone; not one saved time.

7. **`od -c` LIES ABOUT LINE ENDINGS TOO, IF YOU GREP ITS OUTPUT.**
   C3b's trap 7 says use `od -c` instead of `grep -c $'\r'`. But
   `od -c | grep -c '\r'` counts *lines of od output*, not carriage returns,
   and told me a region was CRLF when Python reading with `newline=''` showed
   it was LF. **Read the file in Python with `newline=''`; that is the only
   answer that has been right every time.** The file was genuinely mixed —
   `docs/walkthroughs/27_ui_admin_terminal.md` is CRLF with an LF part three,
   because the append was a shell heredoc.

8. **Two subagents sharing one scratchpad root overwrite each other.** Round
   one reported that a file it had written (`scan.mjs`) was rewritten between
   two of its own tool calls by the other reviewer, and it correctly discarded
   everything downstream of that point. **Give parallel reviewers distinct
   filenames or distinct directories**, and tell them the scratchpad is shared.

9. **The dev server can serve a reviewer's in-flight mutation.** Mid-session a
   measurement came back as 8px and 9px text where the source says 13px. The
   tree was clean at that instant, but a reviewer had been mutating seconds
   earlier and the browser was holding stale HMR output. **A measurement taken
   while another agent has write access to the tree is not evidence.** Re-read
   settled, it was 13px.

10. **The honest screenshot can show nothing.** The Change-requests
    before/after pair is two empty pages — nobody has filed a change request in
    the dev fixture — so every row-level change in that section is invisible in
    it, and the pair would have shipped to Audrey as if it proved something.
    There is a clearly-labelled seeded image beside it now. **Look at the
    screenshot you are about to hand over.**

12. **🚨 I HIT C3b's TRAP 3 AFTER WRITING IT INTO THIS FILE.** `git checkout --
    <file>` to undo a temporary state-forcing edit **also reverted the
    uncommitted fix in the same file**, so a commit shipped claiming a change
    it did not contain — the five card-title inks were still `INK_2` under a
    message describing them as fixed. Caught only because the next mutation's
    anchor did not match and I read the line instead of the count. It is
    amended. **Reverting a force is the same operation as reverting a mutant:
    commit first, and after any `git checkout --`, re-read the file for the
    work you meant to keep.** Knowing a trap is not the same as avoiding it;
    the check is what avoids it.
11. **The C: drive.** 115G free at the start and no incident this session, but
    C3b's trap 12 stands: `df -h /c` before concluding a tool is broken.

---

## 6. Waiting on Audrey, and requests for other lanes

**Waiting on Audrey:** nothing blocking.

1. **A CI status.** Three sessions have owed one. `gh auth login` once in this
   worktree, or read
   `https://github.com/pretty-aud/wilson/actions?query=branch%3Aui%2Fc3c-admin`
   and paste the result. §2 says why it is worth more than the last two
   sessions thought.
2. The walkthrough's part three asks for nothing, and the two questions from
   part one are still open and still not blocking: the ADD PEOPLE menu copy,
   and whether anything stopped responding.

**Kit requests: all five are CLOSED, and not by me.**

🚨 **THE INTEGRATION MERGE SATISFIED EVERY ONE OF C3b's FIVE KIT REQUESTS.**
F4 landed them while this session was running, so the §6 a next session would
otherwise have inherited is empty. Written out so nobody re-files them:

| C3b's request | F4's commit | What landed |
|---|---|---|
| `Row` has no way to say "this state is false" | `e086b33` | a boolean `data-*` is read by its VALUE, never by its presence |
| `Tabs` inside `Toolbar` wraps | `3d3f196` | `.ui-toolbar .ui-tabs { flex-wrap: nowrap }`, contextual, no prop |
| `.ui-th-label` cannot ellipsise | `9f7774d` | a header that does not fit ellipsises instead of shearing |
| a small `Select` loses its arrow clearance | `960e07d` | `select.ui-input[data-size="sm"] { padding-right: 28px }` |
| `Badge` has no size below its default | `798eca5` | an **xs** IconButton — `--control-xs: 16px`, `--icon-xs: 10px` |

**Two of the page-local copies are already retired** (`46c3af0`), because F4's
own rules name them: the tab strip and the small select. Verified in the app
with only the kit rules in play — Logs' two tabs at y=244 on one line, toolbar
at 44px, the severity select at 28px of clearance.

**One is left, and it is ten minutes.** `.at-dept-remove` is a hand-rolled 16px
box with a 10px glyph, built because the kit had no badge below 20px and no
icon button below 28px. F4 shipped `--control-xs: 16px` and `--icon-xs: 10px`
— **the same two numbers**. An `<IconButton size="xs">` in `CompanySection`
retires that whole block AND the single exemption §3.3's stylesheet guard
currently carries. It is not in this session's two files, which is the only
reason it is not done.

**One request this session opens, and the reason is in its own diff.** The
kit's `.ui-section-desc` is the Dense step with `max-width:
var(--measure-reading)` (`index.css:1569`). The `.at-*` cards on this page have
no measure cap, so their paragraphs run **72–80ch** against §3.1's 72ch
ceiling while the section description directly above them runs 58. Going 11 →
13px *improved* that (it was ~95ch before), so this is older than the type
pass — but the kit could carry the measure helper the pages are all
hand-rolling.

## 7. Next session's first steps

```
git fetch origin
git checkout -b ui/<next> origin/feat/ui-overhaul
npm install --ignore-scripts && git checkout -- package-lock.json
```

Copy `.env.local` from `C:\Users\Audrey\Documents\My_Work\Dev_Work\wilson\WILSON\.env.local`
into `WILSON/` and add `VITE_DEV_AUTOLOGIN=tester` and `VITE_DEV_FIXTURES=1`.
Never commit it. **Add the `c3c-worktree` launch entry from §2 before
screenshotting anything** (§5 trap 1).

Read: this file; `docs/design/UI_OVERHAUL_PLAN.md` §0 C1–C9 and §3.1–§3.3;
`docs/sessions/HANDOFF_PROTOCOL.md`. You do **not** need C3b's hand-off —
everything still live from it is repeated here.

### What is left, in order

1. **AT-20, the S3 card's three-block regroup** — Connection / Credentials /
   Verify, an eyebrow per group, a hairline above, 24px between groups against
   8px within, one filled primary per region. The visual-only reframe, not the
   original finding: nothing is hidden and no control moves behind a click.
   **Still blocked on Track C's merge** (37 commits). While you are in there,
   the Prefix label's wrap (§4) is the same card.
2. **Ask Track A about `st.color`** (§4). One attribute, and the answer decides
   whether the status badge on a change-request row has a tone at all.
3. **Get a CI status recorded** (§2, §6). Three sessions owed.
4. **Retire `.at-dept-remove`** onto F4's new `<IconButton size="xs">` (§6).
   Ten minutes, and it closes the last §3.3 exemption on this page. The two
   other page-local kit copies are already retired in `46c3af0`.
5. **The eleven hand-rolled alert strips → `Banner`** (§4), when this file is
   free.

### Things already settled — do not re-derive them

- W9 is a no-op here: zero `window.confirm` in this directory (four sessions).
- There is no `fmtCurrency` in this directory.
- The third `ThLight` is in `src/admin/`, out of scope (Q14).
- No `ShortcutBar` (Q10). AT-23 is dead. AT-06 and AT-24 dissolved with the
  ground flip.
- The `display` ternaries in `AdminTerminalPage` stay.
- **The `w-7 h-7` (28px) icon the C3c chip names does not exist** — zero hits
  anywhere in `src/components/AdminTerminal/`. The chip's other counts were
  right.
- **The Storage card prose is Dense (13px), not Body (14px), and that was
  argued and settled.** A reviewer made the case for Body from §3.1
  ("paragraphs, descriptions") and from leading. It is Dense because the kit's
  own `.ui-section-desc` — the description rendered directly above these cards,
  which is Foundation's code and not this lane's — is Dense; Body would make
  every card paragraph larger than the section's own description. The measure
  argument also runs the other way: going 11 → 13px **reduced** the characters
  per line, from about 95 to 72–80.

---

## 8. The adversarial review rounds

### Round one — two reviewers, one on the design system, one on the guards

Both ran against `1b0d4ae`. Between them: **59 mutations, 34 green, 2 red on
correct code.** Round two added two more green and one more red, for **62 /
36 / 3** across the session. Both numbers were acted on.

**The single most useful finding was a defect I had just introduced**, and both
reviewers reached the picker-label collision independently of each other — one
from the served CSS, one from the diff. See §5 trap 2.

Acted on, with the measurement that made it a defect:

| Finding | Measured | Fix |
|---|---|---|
| `.at-picker-label` at H3 equals the card title above it | 14px/600, and the same ink in the shipped disabled state | Dense at 600 — four distinct levels |
| The two field-label parentheticals inherit the Label tracking | 0.66px, where §3.1 says zero | `.at-label-aside`, measured back to `normal` |
| 34 guard evasions | see below | the guards rewritten |
| `st.color` reads a key `STATUS_TONE` does not have | `{ color: undefined }` | **recorded, not fixed** — §4, merge safety |
| `.at-dept-remove svg` is 10px | against §3.3's 14/16/24 | named as an exemption so it is visible |

**Considered and rejected, with the reason:** Storage card prose to Body (§7);
and the required-field hint at `ChangeRequestsSection.jsx:469` moving from
Caption to Dense — it stays Caption because the kit's own `.ui-field-hint` is
`var(--text-caption)` and that is the precedent for a hint under a field.

**The 34 evasions fell into four families**, and each got a structural answer
rather than a patched regex — a className that is not a double-quoted literal;
another spelling of the same property; **the stylesheet, where eleven of the
thirteen files keep their type**; and an inline `style`. Plus one structural
finding worth its own line: with the arbitrary-size scan asserted empty, the
floor and scale checks could only fail *through* it, because every value in
`STEP_PX` is ≥ 11 and on the scale — **populated and inert, C3b's trap 8 one
level up.** They read four sources now.

**Two guards were red on correct code**, which is a defect of the same size:
converting every icon to Tailwind v4's `size-*` emptied the icon scan, and the
scan read `size=`/`width=` off any capitalised tag, so `<Table width={110}>` (a
column width) and `<Avatar size={40}>` both failed it. It derives from each
file's own `lucide-react` import now.

**All 38 replayed against the rewritten guards: 34 caught by the test named for
them, 4 pieces of correct code left alone.** The replay harness is
`scratchpad/replay.py`; it is disposable but the case list is the record.

### Round two — it measured the running app, and found that correction 1
### satisfied the letter of round one's finding and not its point

Told to attack round one's corrections by driving the page rather than reading
the diff, because round one had already read everything. It verified the server
was this worktree by the HMR path in the served module before measuring
anything, which is the right instinct.

**🚨 THE CARD TITLES WERE THE QUIET INK AND THEIR OWN CHILDREN WERE THE LOUD
ONE.** Round one's correction moved `.at-picker-label` to Dense/600 and I
asserted the section now read as four distinct levels. It did, by size. It did
not by colour: the five Storage card titles carry an inline `color: INK_2` and
`.at-picker-label` is `--color-ink`, so with the pickers enabled the card title
measured **7.85:1** and the option title nested inside it measured **14.29:1**
— the smaller, deeper element reading as the more important one. The inline ink
was not new and was not wrong when it was written: these were 11px uppercase
EYEBROWS until this session, and an eyebrow is the quiet ink. **The role
changed and the ink did not follow it.** Settled by the kit rather than by
taste: `.ui-card-title` is `--color-ink`, and `ChangeRequestsSection`'s row
title — the same `text-h3` — was already `INK`, so one token was painting at
two brightnesses across the two files this session touched. Measured after:

| | shipped (pickers disabled) | enabled |
|---|---|---|
| section | 16/600 ink | 16/600 ink |
| card title | 14/600 **ink** | 14/600 **ink** |
| option title | 13/600 ink-2 | 13/600 ink |
| option blurb | 13/400 ink-2 | 13/400 ink-2 |

**Two more mutants, both green at 89/89, both diff-proved:**

- **Mutant 35 re-admitted this session's own fixed defect.** `fontSize` is
  exempt from the inline-type ban because the size scan reads it — but reading
  it only proves the number is on the scale, and **11 is on the scale**.
  `<h3 className="text-h3" style={{ fontSize: 11 }}>` put a card title at 11px
  above its own 13px body copy, which is verbatim what `1b0d4ae` exists to fix.
  An element told its size by a step may not be re-told inline now; the avatar,
  the one legitimate inline size, carries no step class and is therefore exempt
  by construction rather than by name.
- **Correction 2 had no guard at all.** `grep at-label-aside
  adminTerminalCss.test.js` returned zero. Deleting its two declarations was
  green and measured: the aside goes **168.2px → 199.9px**, stops fitting its
  **223.6px** column, wraps to two lines, and drops the Prefix input **15px**
  below Region's and Bucket's. Pinned now, with a countable invariant on its
  two callers.

**Confirmed correct, with measurements** — as useful as the findings:

- Both Cancel buttons: ghost reached, `border-color rgba(0,0,0,0)` against the
  sibling Continue's `rgba(245,240,236,0.14)`, text **6.49:1** on the
  composited signal panel, hover **6.49 → 9.98:1**, focus ring `2px
  solid #ea580c` at **3.75:1** (SC 1.4.11 wants 3:1) with 3px of extension
  against an 8px gap, and **both still cancel** with the typed root preserved.
- **Nothing below 4.5:1 anywhere**, composited through every translucent layer.
  Floor values: 4.54:1 (the signal-orange secret-status line, and the
  underlined "Open their course"), 4.66:1 (disabled controls, exempt anyway),
  5.04:1 (the chip count), 5.18:1 (the filled primary).
- **No overflow, no double scroll, no clipping at 1280x700.** Exactly one
  vertical scroller; **zero** elements with `scrollWidth > clientWidth`. The
  deepest S3 branch is 2.77 screens at 1280x700 — long, but it scrolls once.
- **Type does not change with window size**: across 6 stylesheets and 2534
  rules, zero media or container queries touch `font-size`, `line-height` or
  `letter-spacing`.
- **Icons: none at 24px, none non-square, at either size.** The four whose size
  class was deleted measure 14x14 with no class at all, so the kit's rule is
  confirmed reaching them.
- **All eleven Storage branches forced and looked at**, and Change requests
  seeded with five rows across all four statuses, both decision panels and the
  Decided tab.

**Two method notes it left, both worth keeping:**

- A hidden Browser pane does not fire `requestAnimationFrame`, so
  `await new Promise(r => requestAnimationFrame(r))` hangs until the tool
  timeout. Use `setTimeout`.
- **CSS transitions are still running when you measure.** Its first hover read
  looked like a completely dead hover; it was the 120ms ease-out mid-flight.
  Wait 200ms after any state change before calling `getComputedStyle`, or you
  will report a false positive.

**One finding recorded rather than fixed:** the two field-label parentheticals
are the only prose in the section still at 11px, inherited from their Label
parent. They stay, and the reason is now in the stylesheet next to the rule:
a parenthetical inside a label is part of the label, and at 13px it would be
larger than the field name it qualifies — the same inversion this session made
twice and fixed twice.

**All 41 mutants replayed against the final guards: 37 caught by the test named
for them, 4 pieces of correct code left alone.**

---

## 9. Auto-memory: none

This worktree's memory folder is its own and empty, and the main repo's is not
this session's to edit. This file is the memory; every lesson is in §5.

---

## 10. Closing state

- Branch `ui/c3c-admin` pushed to origin at `2ae9a21`: twelve of this
  session's own plus two integration merges.
- `feat/ui-overhaul` fast-forwarded to this branch's head.
- 🚨 **GitHub was unreachable once, again** — `Failed to connect to
  github.com port 443 after 21104 ms` on the first push to
  `feat/ui-overhaul`, succeeding on retry a minute later. Fourth session
  running. Plan §6.3's "retry on rejection" covers it, and the retry is also
  when the SECOND merge was noticed, so the failure was useful.
- **vitest 2823 / 139 files** after both merges, green. `npx vite build` clean
  but for the two pre-existing warnings.
- 🚨 **The merge brought F4, which closed all five of C3b's kit requests** —
  see §6. Two page-local copies retired here as a result; one is left and it
  is ten minutes (§7 item 4). The merge was clean: no conflicts, in either of
  the two track-owned files or anywhere else.
- Both review rounds closed. Round one's corrections are in `2bfa167` and
  `d873c90`; round two's are in `1878e99`. Every evasion either round
  demonstrated has been re-run against the repaired guard and now fails, and
  the four pieces of correct code that a guard was blocking now pass.
- `adminTerminalCss.test.js`: 66 (C3) → 71 (C3b) → **91** here.
- Screenshots re-taken twice, after each of the two design corrections — C3b
  recorded shipping a set captured six commits early and the reviewers finding
  defects in it.
- No chip spawned; the controller sequences lane C.
- Dev server on 5222 stopped; Browser pane viewport reset.
- Last command: `git checkout --detach`.
