# Hand-off — UI overhaul, Wave 3 lane B bundle B4 (R.A.B.B.I.T. assets and files), session one (B4), 2026-09-25 — **mid-bundle: surfaces 1 and 2 of 7 are done**

Model: Opus 5.5 (`claude-opus-5-5`). The protocol's context trigger fired
(far more than 300 KB of files and tool output read: the R4 review alone is
100 KB, and this surface is 8,557 lines), so this session hands off at a
clean seam, as the brief anticipated. **The controller spawns B4b.** No chip
was spawned.

## 1. Where you are

- Lane B, bundle B4 (plan §5: `ProjectAssetsView`, `FileManager`,
  `RelationsPanel`, `ProjectFilesTable`, `FileThumbnail`, `VideoPreview`,
  the file tables onto `Table`, Levels, Experiences).
- Branch `ui/b4-rabbit-files`, pushed to `origin/feat/ui-overhaul`: the code
  at **`10ee1f2`**, then the docs commit carrying this file (fast-forward
  both times: nobody else pushed; `origin` was still at B3d's `91eaa4e`).
- Commits, oldest first:
  - `aa286df` dev(fixtures, walk): the `?fixtures=game` variant and sixteen
    walk screens.
  - `7f2f60f` surface 1: `ProjectFilesTable` onto the kit `Table`; the kit
    `CellSelect` (B2's kit request).
  - `d1a7396` surface 1 follow-up: every column holds its header; a narrow
    frame takes 8px cells; the walk guards the named trash buttons.
  - `3af5cd6` surface 2: `FileManager` onto the kit; the delete question on
    the kit Dialog (W9); the notes editor owns its Escape.
  - `10ee1f2` surface 1 follow-up: in a narrow frame the file name keeps the
    room (found by the element screenshots).
  - the docs commit carrying this file and the screenshots.
- The bundle is **not** closed: surfaces 3–7, `BinFileTable`, both review
  rounds and walkthrough 44 are B4b's (§4).

## 2. State, measured

- **Vitest: 159 files / 3316 tests**, green; with `.env.local` moved aside,
  the same (B3d's hand-off: 154 / 3268; B4 added five files — `gameVariant`,
  `CellSelect`, `rabbitFilesCss`, `rabbitFilesRender`,
  `rabbitFilesManagerRender` — and 48 tests).
- **CI**, "RLS tests": `10ee1f2` (the last code push) run
  [36114802579](https://github.com/pretty-aud/wilson/actions/runs/36114802579)
  **success**. One of three anonymous reads used; the docs commit's own run
  is in the chat report.
- **`ui-audit.mjs --css`**, now **12 sheets** (`rabbitFiles.css` added):
  every row unchanged — hex 9 in 1 (all `index.css`'s `.companion-chat-md`,
  P1's), border >= 2px 19 in 7, radius 3 in 1, weight 1 in 1, off-scale
  size 3 in 1 — and `rabbitFiles.css` scores **0 on every row**
  (`typeScale.test.js` carries its real-path control).
- `inlineStateTernaries`: `ProjectFilesTable` **0**, `FileManager` **0**.

### The walk — before and after

The **before** was measured on a clean worktree at `aa286df` (the base code
plus the fixtures variant and the walk screens) served on port 5263, never
on the working tree (§5 trap 1). Both sizes read the same except `clip`.

`node scripts/ui-walk.mjs <port> 1440 900 --only <screens> -v` (and 1280 700):

| screen | before (both sizes) | after surfaces 1–2 |
|---|---|---|
| **rabbit-settings** | cr **96**, rad **1**, b2 2 | **cr 0, rad 0**, b2 2 |
| **rabbit-help** | cr **96**, rad **1**, b2 1 | **cr 0, rad 0**, b2 1 |
| rabbit-timeline | cr 0, b2 5, clip 20 | unchanged |
| rabbit-task-history | cr 0, b2 2 | unchanged |
| shell-nav | 0 | 0 |
| rabbit-summary | cr 96, rad 1 | **cr 0, rad 0** |
| rabbit-control-panel | cr 96, rad 33, anon 32 (known) | **cr 0, rad 0, anon 0** (KNOWN deleted) |
| projects-detail | cr 90, rad 31, anon 30 (known) | **cr 0, rad 0, anon 0** (KNOWN deleted), clip 0 → 2 (long names ellipsise, titled) |
| rabbit-intake | cr 0 (no file picked; the table below was measured with one) | cr 0 |
| rabbit-task-detail | cr 1, c6 1 (known "Add files") | **cr 0, c6 0** (KNOWN deleted), b2 3 (the kit Tabs' underline) |
| rabbit-files-audit | cr 99, rad 33, anon 32 | **cr 3, rad 0, anon 0** — the 3 are the drawer's own (surface 7) |
| rabbit-asset-detail | cr 24, c6 4, rad 15, anon 32, nest 2 | cr 23, c6 3 (FileManager's share gone; the rest is the popup and the table behind it, surface 3), b2 5 |
| rabbit-scene-detail | cr 55, c6 4 | cr 53, c6 3 (FileManager's share gone; the rest is B5's popup), b2 4 |
| dashboard-task-detail | 0 | 0 |
| files-project | cr 0 | cr 0 |

`rabbit-control-panel`'s `clip` went 33 → 37 (the narrow table ellipsises
long names at 97px, each with its title) — a compare number, not a failure
(`ui-measure.mjs`). No KNOWN entry is stale after the five deletions.

The five screens B3 walked: **Settings and Help read 0 contrast lines and 0
radii** at 1440x900 and 1280x700 (from 96 and 1: they were the Summary's
`ProjectFilesTable`, measured under both panels — B3d §5 trap 1).

The B4 screens not yet restyled read, before, at 1440x900 (1280x700 the
same but `clip`):

| screen | before |
|---|---|
| rabbit-assets | cr 5, c6 2, rad 15, anon 31 (known 33), clip 15 |
| rabbit-assets-gallery | cr 20, c6 2, clip 11 |
| rabbit-asset-new | cr 14, c6 2, rad 15, anon 32 (known 34) |
| rabbit-asset-warning | cr 6, c6 3, rad 15, anon 32 |
| rabbit-asset-link | cr 24, c6 4, rad 15, anon 32, nest 2, err 2 |
| rabbit-asset-files-gallery | cr 29, c6 4, rad 15, anon 34, nest 2, err 2 |
| rabbit-asset-video | cr 24, c6 4, rad 15, anon 32, nest 2, err 2 |
| rabbit-files-audit | cr 99, rad 33, anon 32 |
| rabbit-levels / rabbit-experiences | cr 10, c6 2, b2 5 / 4, anon 1 |
| rabbit-levels-gallery / experiences-gallery | cr 9 / 8, c6 2, anon 5 / 4 |
| rabbit-level-detail / experience-detail | cr 21, c6 2, nest 2, err 1 |
| rabbit-level-assets / experience-assets | cr 21, c6 2, nest 2, err 1 |
| rabbit-level-task / experience-task | cr 36, c6 2, nest 2, err 1 |
| rabbit-bins | cr 0, clip 9 |

Screenshots, in `docs/sessions/handoffs/img/`:
- `b4-before-<screen>-<size>.png` — 25 walk screens x 2 sizes, from the
  clean worktree (B4's screens plus Settings and Help).
- `b4-after-<screen>-<size>.png` — the 10 walk screens surfaces 1–2 changed.
- `b4-after-files-table-{summary,control-panel,projects-detail}-<size>.png`
  and `b4-after-file-manager-{asset-table,asset-gallery,delete-confirm,
  task-column}-<size>.png` — ELEMENT shots, because the walk's viewport shots
  cannot see either component (below the fold in its page or popup). Taken
  with `scratchpad/b4-element-shots.mjs` (Playwright, the walk's browser).

### The four `ProjectFilesTable` renderers, measured in the running app

| surface | width | rows | text under 4.5:1 | clipped | radii | sideways scroll |
|---|---|---|---|---|---|---|
| Summary (read-only card) | 1348 | 32 at 32px | 0 of 231 | 0 | 3px | 0 |
| Control Panel (`ProjectFilesSection`) | 645 | 32 at 32px | 0 of 233 | 0 | 3px | 93 (the old grid scrolled too); Name 125 |
| Projects page detail | 1230 | 30 at 32px | 0 of 210 | 0 | 3px | 0 |
| Intake (with a 1.1 MB `.markdown`, the worst row) | 686 | 2 at 32px | 0 | 0 | 3px | 0; Name 125, Description 97 |

All four draw the same component with the same header, row, cell and ink;
`rabbitFilesRender.test.jsx` pins the four call sites (no variant, no size
mapper). The Files page (`ProjectFilesExplorer`, lane C1's) is the same kit
`Table`, `dense`, so its rows match (32px, 12px cells, the Label header).

### Per-file counts (A3's method: template-literal classNames / palette utilities / hexes; plus inline styles and state ternaries)

Counted in code, comments stripped, with `scratchpad/b4-count.mjs` (copied
to §7 for B4b):

| file | lines | before | after | styles | ternaries |
|---|---|---|---|---|---|
| ProjectFilesTable | 401 → 261 | 0 / 1 / 39 | **0 / 0 / 0** | 20 → 1 (the caller's height, as a custom property) | 12 → **0** |
| FileManager | 1224 → 1147 | 0 / 10 / 56 | **0 / 0 / 0** | 42 → 1 (the copy bar's %) | 5 → **0** |
| ProjectAssetsView | 2357 | 0 / 67 / 311 | — (B4b) | 195 | 40 |
| RelationsPanel | 1114 | 0 / 44 / 174 | — | 125 | 35 |
| FileThumbnail | 155 | 0 / 0 / 6 | — | 5 | 0 |
| VideoPreview | 207 | 0 / 1 / 12 | — | 10 | 0 |
| LevelsView | 1296 | 2 / 60 / 222 | — | 141 | 18 |
| ExperiencesView | 1296 | 2 / 60 / 222 | — | 141 | 18 |
| RelinkDialog | 321 | 0 / 3 / 60 | — | 42 | 1 |
| FileAuditDrawer | 171 | 1 / 2 / 32 | — | 20 | 2 |
| AssetStatusWarningModal | 153 | 0 / 1 / 27 | — | 16 | 0 |
| bins/BinFileTable | 161 | 2 / 0 / 0 | — | 22 | 2 |

No file on this surface has an arbitrary `text-[Npx]` or a size under 11px
any more (Wave 1 did the type): R4-02's 7px is gone. What is left is colour,
chrome and structure.

## 3. Done and verified

- **`aa286df` — the tools B4 needed to measure its own surface.**
  - `?fixtures=game` (`src/dev/fixtures/store.js`, `applyGameVariant`):
    Salt Hours with levels and experiences on, four levels, three
    experiences, five assets linked. Levels and Experiences could not be
    opened on the fixtures at all. Loaded only with the query, so every
    other screen's dataset is byte-identical (`gameVariant.test.js`).
  - Sixteen walk screens: the assets gallery, the status-warning modal, the
    relation picker, the asset popup's file gallery, the video player, the
    file-activity drawer, and Levels / Experiences (table, gallery, detail,
    asset picker, new-task panel). Each is proven open by something only it
    renders. `@in:<text>::<label>` clicks the control NEAREST a text (the
    Storyboards row's details button — asset 4 holds the video); `query`
    loads a URL switch without making it the route.
- **`7f2f60f` + `d1a7396` — surface 1, `ProjectFilesTable`.** The kit
  `Table` (`dense`), one language (the warm arm had no caller), every cell
  on the ink ladder, Kind the kit `CellSelect`, Size numeric, every icon
  button the kit `IconButton` named for its file, radii the kit's, an
  `EmptyState` for an empty list, column widths measured for their values
  AND their headers, a container query for frames under 740px (8px cells,
  Description at its header's 97 and Name the rest, never under 125: Intake
  holds the table with no scroll). **Size reads `size_bytes` for all
  four callers**: Summary's `withDisplaySize` and the Projects page's cloud
  mapper are deleted; Intake's picked files and the Projects page's legacy
  local rows carry `size_bytes` where they are made. KIND "—" for cloud
  rows stays a recorded data gap. Verified: §2's table; the walk; the guards
  and the mounted test.
- **`7f2f60f` — the kit `CellSelect`** (plan §5 item 5; B2 §4's kit
  request): a borderless 28px select that reads as the cell's text until the
  row is hovered or focused; its text on the column's edge; the padding that
  keeps a 36 or 32px row (border box: 4/3, dense 2/1).
- **`3af5cd6` — surface 2, `FileManager`.** Header on one 28px baseline
  (Label-role title, `IconButton`, the kit primary "Add files" — white on the
  signal fill, 5.2:1, C6 — and the kit `Tabs` over a named tabpanel), the
  three notices the kit `Banner`, the progress bar on tokens, the kit
  `EmptyState`, the table the kit `Table` (36px rows for the 32px tile,
  Version the kit `Badge`, Size numeric, the date in the mono, named
  Download/Delete), the gallery cards on raised paper. **W9**: Delete asks
  on the kit `Dialog`, portalled into `<body>` (§5 trap 3) — measured
  centred over the popup at z 70, Cancel focused, Escape closes only it,
  Delete deletes. **The notes editor marks its own Escape**, so
  `TaskDetailPopup`'s `markFilesEscape` is deleted (B2 §4); B2's two tests
  follow the new contract. Measured in the asset popup: 0 failures in the
  table; the one in the gallery is `FileThumbnail`'s (§4).
- **Walk KNOWN**: five lines deleted because their defects are gone — c6
  "Add files" on rabbit-asset-detail, rabbit-scene-detail and
  rabbit-task-detail; anon trash on projects-detail (30) and
  rabbit-control-panel (32). The walk now guards them.

## 4. In flight: surfaces 3–7 — B4b's

Nothing uncommitted. `.claude/launch.json` carries two local entries —
`b4-worktree` (5262) and `b4-before` (5263, the clean baseline worktree,
removed at the end of this session) — never staged.

**What is left of the brief, in its order** (one commit per surface; each
file joins `FILES` in `rabbitFilesCss.test.js` and gets a describe in a
mounted test in the commit that moves it):

3. **`ProjectAssetsView`** (2,357 lines: toolbar, filter panel, saved-views
   dropdown, the save-view modal, `AssetTable`/`AssetRow` with the bulk bar
   and groups, `AssetGallery`/`AssetCard`, `NewAssetPopup`,
   `AssetDetailPopup`, `TaskRowInPopup`, `PropField`, the two inline text
   editors, `InlineSelect`, `statusTone`/`toneColors`). The toolbar is the
   twin of B2's Tasks toolbar: compose it the way `ProjectTasksView.jsx`
   ~590–690 does (kit `Toolbar`, native selects in `ui-input` with
   `data-active`, kit `Tabs` for Table/Gallery, a search wrapper), in
   `rb-asset-` classes. Status onto the kit `STATUS` map (R4-05: delete
   `statusTone`/`toneColors`); the table onto the kit `Table` (R4-01, R4-03
   name weight, R4-04 one thumbnail column width, R4-17 numerics right and
   tabular, R4-21, R4-22, R4-42); date inputs `color-scheme: dark` (R4-28);
   the two popups onto the kit `Dialog` (the detail popup is `workbench`
   960 or wider by number; the new-asset popup `form`); R4-26's dead
   related-asset click is RECORDED for P1, not changed (C1). The detail
   popup hosts `FileManager` — once it is the kit Dialog, FileManager's
   quiet text could go back to the third ink (§5 trap 6) only if the scene
   popup has moved too (B5).
4. **`RelationsPanel`** (1,114): the sidebar(s) (360 vs 320, R4-23), the
   pickers (R4-43 visual only), `RelationBadge` — render it in the asset
   popup's four relation fields or delete it (R4-27) — and the nested button
   (R4-24; walk `nest 2` / the React error on the detail screens).
5. **`VideoPreview`** onto the kit `Dialog` (or registered on `overlay.js`'s
   stack), **autoplay untouched (C1)**, portalled like FileManager's confirm
   (§5 trap 3); then delete `filesLayerOpen` and the `onBeforeClose` in
   `TaskDetailPopup` (B2 §4). **`FileThumbnail`**: its extension label is
   `#78716c` (3.65:1 in FileManager's gallery — the one failure left there);
   the five preview geometries (R4-35) recorded or unified without a view
   change. Read `thumbnails.test.js` 560–690 and `videoThumbnails.test.js`
   1000–1400 first: they pin `FileThumbnail` and `VideoPreview` text.
6. **`LevelsView` / `ExperiencesView`**: fold the verbatim twin into one
   component if it can be done with no view change (R4-11), else restyle
   both identically and record R4-11. Walk them with `?fixtures=game`.
7. **The overlays left (R4-12)**: `RelinkDialog` (Dialog; reachable only in
   Local Server mode with missing files — measure with a render test),
   `FileAuditDrawer` (the kit `Drawer`, `xl` 420; walk `rabbit-files-audit`),
   `AssetStatusWarningModal` (Dialog; R4-30's green chip onto `STATUS`),
   the save-view modal, and every `window.confirm` on these files (W9).
- **`BinFileTable`** (B6 §4: "still a CSS grid of 16 columns, not the kit
  `Table`" — the fourth file table the plan converges in B4).
- **Two adversarial review rounds** (`model: "opus"`; round two re-measures
  round one's corrections in the running app, every overlay opened, all four
  file-table renderers), then walkthrough **44**
  (`docs/walkthroughs/44_ui_rabbit_files.md`; 42 is the Timeline's, 43
  A4's) copied to `C:\Users\Audrey\Desktop\WILSON walkthroughs\UI overhaul\`,
  the after screenshots, the closing hand-off and report.
- **Suggested split**: B4b takes 3 and 4 (the Assets view and its relations
  are one screen); B4c takes 5, 6, 7, `BinFileTable`, the reviews and the
  walkthrough. Hand off earlier if the trigger fires.

### 4.1 What B5, V2 and P1 inherit (so far)

- **Kit `CellSelect`** (filled): B5's money tables' inline selects should
  use it. Two older copies remain — B2's `.rb-task-cell-select` /
  `.rb-task-cell-editor` and the Dashboard's `.dash-cell-editor` — for P1
  to migrate (a view-free swap).
- **B4-KR-2 (open), a `portal` option on the kit `Dialog`**: a component
  nested in a host that centres with `transform` (the asset and scene
  popups) cannot render a Dialog as the host's sibling (B3c's rule).
  FileManager's delete confirm portals by hand (`createPortal`); B4b's
  `VideoPreview` will too.
- **B4-KR-3 (open), a compact cell padding on the kit `Table`**: the kit's
  12px cells made the files table ~60px wider than the grid it replaced;
  `rabbitFiles.css` answers with a lane container query (8px under 740px,
  560px for FileManager). If a second lane needs it, it belongs in the kit.
- **B3d-KR-1 (still open), a kit Checkbox**: `ProjectFilesTable`'s Core
  tick is a native checkbox in `accent-color: var(--color-signal)`.
- **B5**: the scene popup hosts `FileManager` on `#292524`; FileManager's
  quiet text is the second ink for that reason (§5 trap 6). The walk's c6
  "Add files" line on rabbit-scene-detail is deleted (fixed). ScenesView's
  popup centres with `transform` (§5 trap 3).
- **V2 / P1**: the `?fixtures=game` variant and the sixteen walk screens;
  `rabbitFiles.css` in the audit; B3d's §4.1 list carried unchanged.

## 5. Traps hit

1. 🚨 **A BASELINE WALK THAT OVERLAPS AN EDIT MEASURES THE EDIT.** Vite
   hot-reloads the running app: the first "before" walk here ran while
   `ProjectFilesTable` was being rewritten and measured the new table on its
   later screens. Measure "before" from a separate worktree at the base
   commit on its own port (`git worktree add --detach <scratch> <sha>`,
   its own `npm install --ignore-scripts`, a launch entry, a marker), or
   finish the baseline before the first edit.
2. ⚠️ **`git update-index --cacheinfo` takes its path from the REPOSITORY
   ROOT**, not the cwd: prefix `WILSON/`. It is how this session committed
   one surface at a time when two surfaces shared a file (stage a generated
   version of the shared file, leave the working tree whole).
3. 🚨 **THE ASSET AND SCENE POPUPS CENTRE WITH `transform`**, so any
   `position: fixed` child — `VideoPreview` today, a nested kit Dialog —
   lays out inside the popup's box, not the window. The kit Dialog does not
   portal; FileManager's confirm is `createPortal(…, document.body)`
   (React events still bubble through the tree it was written in).
4. ⚠️ **A COLUMN HOLDS ITS HEADER, NOT ONLY ITS VALUES.** The Label step with
   its tracking: "CORE" 33.4px, "VERSION" 53.4, "ACTIONS" 52.9,
   "DESCRIPTION" 81. Sized for the 16px tick, Core cut its header; the
   walk's `clip` count (a compare-between-runs number) caught it.
5. ⚠️ **THE KIT'S 12px CELLS ARE WIDER THAN THE GRIDS THEY REPLACE** (8px
   per column): Intake's 686px could not hold the table. A lane container
   query gives narrow frames 8px cells (KR-3).
6. ⚠️ **THE THIRD INK FAILS ON THE LEGACY `#292524`** (4.37:1; 4.66 on the
   kit Dialog's raised paper, 5.04 on paper). A component drawn inside a
   popup that has not moved yet uses the second ink for quiet text.
7. 📌 **A CELL'S `height` IS ITS BORDER BOX**: a 28px control keeps a 36px
   row with 4px above and 3px below (the hairline is the other 1px), 2 and 1
   in a dense row. At 8px the row measured 45.
8. 📌 Levels and Experiences exist only with `?fixtures=game`; a walk screen
   passes it as `query`, not in the path (the app routes to `/rabbit`).
9. 📌 The walk's `dialog: '<text>'` proof reads `innerText`, so a title set
   in capitals by CSS fails a sentence-case text proof (the file-activity
   drawer's "FILE ACTIVITY"); use `dialog: true` where nothing else is open.
10. 📌 Heredocs lose a backslash level (B3d trap 7): every script here was
    written with the Write tool.

## 6. Waiting on Audrey (W14: nothing blocks; carried to the end)

B3d's 27 are the Timeline's and stay in its walkthrough. Carried, touching
these files:

1. (B1, question 4) **KIND for cloud files**: should cloud uploads carry the
   intake's `document_kind`? The column shows "—" for every cloud row (a
   data gap, not aliased).
2. (B2, question 1) The kit Dialog adds a ✕ to question dialogs that had
   none — FileManager's delete question (was `window.confirm`) has one now.
   Keep?

New:

3. **The task popup's files column is 400px, and the file table needs about
   500 at legible widths**: it now scrolls sideways (every column reachable)
   where it used to cut off Download and wrap the size. Widen the column
   (B2's layout), or accept the scroll?
4. **FileManager's view switch is now two words, "Table" and "Gallery"**
   (the kit Tabs, as on Tasks), where it was two unlabelled icons. Keep?
5. **The batch-upload summary is now the neutral notice**, not amber: it
   says nothing failed ("N files have no preview"). Keep, or amber?
6. **The files table's column names**: Kind lists kinds in sentence case
   ("Pitch bible", "GDD") where they were lower case ("pitch bible", "gdd").
   The stored values are unchanged. Keep?

## 7. Next session's first three steps (B4b)

1. In a fresh worktree cut from `main`:
   ```
   git fetch origin && git checkout -b ui/b4b-rabbit-files origin/feat/ui-overhaul
   npm install --ignore-scripts && git checkout -- package-lock.json
   ```
   Copy `.env.local`, add `VITE_DEV_AUTOLOGIN=tester` and
   `VITE_DEV_FIXTURES=1`; a launch entry on a free port (5241–5246 and
   5251–5263 are taken), Vite's root positional, proved with a marker.
   Expect **159 files / 3316 tests** or more.
2. Read this file (§4 is your list, §5 the traps), B3d's §4.1 and §5, then
   the R4 review's findings for your surfaces (`r-a-b-b-i-t-part-4.md`).
   The before screenshots and walk numbers are here (§2); for a live
   before, cut a worktree at `aa286df` on its own port (§5 trap 1).
3. Count with this session's method before editing (the script, verbatim,
   is below) and start surface 3, the Assets view, from B2's toolbar in
   `ProjectTasksView.jsx`; each surface joins `rabbitFilesCss.test.js`'s
   `FILES` and a mounted test.

`b4-count.mjs` (run from `WILSON/`; it imports the guards' `jsCode` and
`inlineStateTernaries`):

```js
import { readFileSync, existsSync } from 'node:fs'
import { pathToFileURL } from 'node:url'
import { join } from 'node:path'
const g = await import(pathToFileURL(join(process.cwd(), 'src/tools/rabbit_v0.1.0/rabbitCssGuards.js')).href)
const R = 'src/tools/rabbit_v0.1.0/'
const FILES = ['components/ProjectFilesTable.jsx', 'components/FileManager.jsx', 'views/ProjectAssetsView.jsx',
  'components/RelationsPanel.jsx', 'components/FileThumbnail.jsx', 'components/VideoPreview.jsx', 'views/LevelsView.jsx',
  'views/ExperiencesView.jsx', 'components/RelinkDialog.jsx', 'components/FileAuditDrawer.jsx',
  'components/AssetStatusWarningModal.jsx', 'views/bins/BinFileTable.jsx']
const PAL = 'slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose|white|black'
const PAL_RE = new RegExp(`(?<![\\w-])(?:[a-z-]+:)*(?:text|bg|border|ring|accent|outline|divide|placeholder|fill|stroke|from|to|via|decoration|caret)-(?:${PAL})(?:-\\d{2,3})?(?:\\/\\d+)?(?![\\w-])`, 'g')
for (const f of FILES) {
  const src = readFileSync(R + f, 'utf8').replace(/\r\n/g, '\n'); const code = g.jsCode(src)
  const n = (re) => (code.match(re) || []).length
  console.log(f, 'tl', n(/className=\{`/g), 'pal', n(PAL_RE), 'hex', n(/#[0-9a-fA-F]{3,8}\b/g),
    'styles', n(/style=\{\{/g), 'ternaries', g.inlineStateTernaries(src).length)
}
```

## 8. Auto-memory: none.
