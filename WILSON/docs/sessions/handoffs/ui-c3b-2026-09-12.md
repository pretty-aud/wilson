# Hand-off — UI overhaul C3b (Admin Terminal, the conversion), 2026-09-12

Model: Opus 5 (`claude-opus-5`). The chip named Opus 5; the session opened on
Fable 5.1, said so on line one and stopped before touching anything; Audrey
switched the picker and the work began after that. Same opening as C3's.

**Bundle C3 is finished.** C3 handed off mid-bundle with the state extraction
landed and no file converted; this session converted all thirteen, flipped
the ground, and closed the twelve-item list C3's §7 left.

---

## 1. Where you are

| | |
|---|---|
| Lane / bundle | C, bundle **C3**, completed by session **C3b**. Plan §5 Wave 3. |
| Branch | `ui/c3b-admin`, cut from `origin/feat/ui-overhaul` at `dd4fdcd` |
| Last commit | `ae53503` |
| Integrated? | see §10 |
| Merged to `feat/multi-user-v1`? | No, and must not be (plan §6.4). |

Commits, oldest first:

```
b45b2f8  test(admin): the render tests C3 owed — the state attributes carry the right boolean
55f82bc  feat(admin): the Admin Terminal onto the kit and onto `paper`
673b2dd  fix(admin): every roster row painted as selected, and three things the screenshots showed
ae53503  fix(admin): the last four sub-floor sizes, and a guard that keeps the floor
280c9d3  fix(admin): Enter stopped submitting the create-user dialog
a2ad448  fix(admin): review round one — five unreadable or dropped states, and four guards that could not see them
3c20524  fix(admin): three sections had a scroll container inside a scroll container
98148a3  fix(admin): the mono was still the face of twelve messages and paragraphs
e4f2619  fix(admin): review round two — a password could be lost in one action, and four guards it walked straight through
d12a74d  test(admin): the create-user keyboard path, split across the two environments
```
plus two docs commits and this file.

🚨 **Eight of the twelve are fixes to the conversion, and not one of them came
from the test suite**, which was green before and after each. They came from
driving the page, from the plan's own grep audit, and from two reviewers
measuring. The suite's job here turned out to be holding the fixes, not
finding the defects — which is worth knowing before trusting a green run on
the next bundle.

---

## 2. State, measured

- **vitest: 2720 passed / 137 files**, `CI=true npx vitest run`.
  C3 closed at 2605 / 136; F3 integrated at 2694 / 136. This session adds
  `adminTerminalState.test.jsx` — 18 tests, the 137th file — and rewrites
  `adminTerminalCss.test.js` from 66 to 71. The rewrite is not a loss of
  coverage: the 35 pinned colour literals it carried belonged to a sheet that
  now has none, and they are replaced by C8 assertions plus the guards the two
  review rounds proved were needed. The pair count fell from 19 to 15 because
  five of those states moved into the kit, where a component test holds each.
- **Run WITHOUT `.env.local`** (`mv .env.local .env.local.aside; CI=true npx
  vitest run; mv .env.local.aside .env.local`): **green**, re-run at the head.
  This is the CI shape: the new render tests mock `supabaseClient`, which is
  constructed at module load and throws `supabaseUrl is required` without it.
- **`npx vite build`**: clean, four times across the session. Only the two
  pre-existing warnings (sentry.js dynamic-vs-static import, chunk size).
- **CI run URL: not available.** `gh` is not authenticated in this worktree
  (`gh auth login` / `GH_TOKEN` both absent) and C3 could not retrieve one
  either. The runs are at
  `https://github.com/pretty-aud/wilson/actions?query=branch%3Aui%2Fc3b-admin`.
  **This is now two sessions in a row owing a CI status; the next session
  should get `GH_TOKEN` set or ask Audrey to read the run.**
- **Migrations / Edge Functions / pgTAP:** none. UI-only; nothing applied to
  dev, staging or prod, no function deployed.
- **Dev server used for verification:** a `c3b-worktree` entry added to the
  worktree's own `.claude/launch.json` (untracked, so the next worktree will
  not have it — the whole entry is below). See §5 trap 1: the `wilson-dev`
  entry already in that file serves the **wrong checkout**, and the app boots
  and the page renders and none of it is your code.

  ```json
  {
    "name": "c3b-worktree",
    "runtimeExecutable": "npx",
    "runtimeArgs": [
      "vite",
      "<ABSOLUTE PATH TO YOUR WORKTREE'S WILSON DIRECTORY>",
      "--port", "5221", "--strictPort"
    ],
    "port": 5221
  }
  ```

  Vite takes its root **positionally**; `--root` is an unknown option and
  exits 1. Confirm before trusting anything you see:
  `curl -s http://localhost:5221/src/ui/Table.jsx | head -c 40` must return a
  module, not `<!doctype html>`. Server stopped at hand-off.

---

## 3. Done and verified

### `b45b2f8` — the debt C3 recorded, paid first

C3's §7.0: the source-scan guard proves each state is written as
`String(<expression>)` but cannot prove the expression is the right boolean.
Seven tests mount the real components against F1's harness and assert each
attribute against the prop that drives it, both ways round. Proved against
four mutants, each caught:

```
data-on       !!on           -> !on             3 failed
data-inactive !m.is_active   -> !!m.is_active    1 failed
data-selected selectedId===  -> true             1 failed
data-invalid  !usernameOk    -> !!usernameOk     1 failed
```

No `jest-dom` in this repo, so the assertions are `getAttribute`, which is
also what `src/ui/Table.test.jsx` uses.

🚨 **These tests then caught nothing, and that is the lesson.** They passed
through the whole conversion — including the commit where every roster row
painted as selected. See §5 trap 2.

### `55f82bc` — the conversion and the ground flip, one commit

The registry rule is that `surface` names what a page PAINTS TODAY, so the
flip and the inks are atomic or the page renders black on black.

All twelve items from C3's §7 list, verified by grep after the fact:

| # | Item | State |
|---|---|---|
| 1 | `pages.js` → `surface: 'dark'` + `measure: 'data'` | done |
| 2 | `pages.test.js` dark list + `stillLight` rewritten | done |
| 3 | duplicate `<h1>` gone, page padding gone, nav grouped | done |
| 4 | `SectionTitle` on all seven sections | 7 / 7 |
| 5 | `Button` / `IconButton`; the four `darkBtnClass` pairs | 46 sites; 0 private pairs |
| 6 | `Table` / `Th` / `Td` / `Row`, widths declared | 4 tables; 0 `ThLight` |
| 7 | `Dialog` on all four overlays | 4 / 4 |
| 8 | `Banner`, `Loading` vs `EmptyState` | 8 files / 3 / 4 |
| 9 | `StatusBadge` 2, `StatusDot` 3, `Switch` 1, `Menu` 1, `Chip` 2, `Card` 4, `Toolbar` 2 | done |
| 10 | type and case | 0 off-scale sizes; 91 uppercase runs → 14 |
| 11 | `../lightSurface` imports | 0 / 13 |
| 12 | `TRANSCRIBED_HEXES` → 0 | 0 (one `#f4a261` survives in a history comment) |

The fourteen surviving uppercase runs are all in `StorageSection` (12) and
`ChangeRequestsSection` (2) — the two files scoped to tokens and kit
components only, where the type and case pass cannot happen until Track A and
Track C merge. See §4.

**W9 is a no-op here, verified a third time.** `grep -rn "window.confirm"
src/components/AdminTerminal/` returns zero. The page's confirmations are
in-flow multi-step states and the review calls two of them good interaction
design; converting them would be a C1 behaviour change. **There is no
`fmtCurrency` in this directory** either — zero hits; the four copies the
plan names are in `RateCard/importers/` and three R.A.B.B.I.T. budget tabs,
other lanes'. **The third `ThLight` is `src/admin/CompaniesSection.jsx`**,
the operator console, out of scope under Q14 — the chip named it as in-scope
("Companies, Logs, Users") and it is not.

### `673b2dd` — what the running app showed and no test could

Three defects, all found by looking rather than reading. The first is in §5
because it is a lesson, not a detail.

1. Every roster row painted as selected AND deactivated.
2. AT-10 was two sections short — Storage had its own uppercase `<h2>`,
   Change requests had no heading at all.
3. `.ui-tabs` is `flex-wrap: wrap`, right for R.A.B.B.I.T.'s eleven tabs and
   wrong inside a `Toolbar` whose slot is `min-width: 0`: Logs stacked
   *System* over *Activity* and the toolbar lost its one baseline. Scoped to
   this page's toolbars, not fixed in the kit, because eleven tabs do need to
   wrap. **Kit request in §6.**

`scripts/screenshot.mjs` gained `--clicks` and `--slug`: the seven sub-views
have no URL of their own.

### `ae53503` — the 11px floor

Three `text-[10px]` raised; the fourth was the Decline button, the last
hand-written secondary on the surface, sitting beside an Approve that was
already the kit's. Four new assertions hold the floor, the scale, and the
stylesheet's use of `--text-*`, plus one that fails if the scan goes empty.

### Screenshots

`docs/sessions/handoffs/img/c3b-after-{admin-terminal,storage,logs,requests}-{1440x900,1280x700}.png`,
paired with C3's `c3-before-*`. 🚨 **The data is the dev fixture** — eight
invented members — and Logs and Storage show the RLS refusal a tester session
gets, not data. That refusal is where AT-05's 2.14:1 error text is now
plainly legible, so the before/after pair is the finding photographed.

---

## 4. In flight

Nothing uncommitted; the tree is clean apart from the app's untracked
`.claude/` and the gitignored `.env.local`.

**What is NOT done, and why, precisely:**

`StorageSection.jsx` (Track C) and `ChangeRequestsSection.jsx` (Track A) took
**tokens and kit components only, no JSX restructuring**, because that is
what their chips require. Their colours, their buttons, their inputs and
their section headings are converted; their **type and case are not**. That
is 12 uppercase runs and 38 `text-[11px]` in Storage, 2 and 9 in Change
requests, plus five more behind the `.at-prose` class this session introduced
when it took five inline colours off the Storage pickers — **52 sites of body
copy at the 11px Label step**, which plan §3.1 reserves for table headers,
field labels, eyebrows and status badges. Round one named it the largest
remaining readability gap on the surface, and it is.

🚨 `.at-prose` ENCODES THE GAP IN CSS, deliberately and with a comment saying
so. It exists because those five strings needed a class rather than an inline
colour (the disabled state could not reach them otherwise), and it holds the
CURRENT size so the fix stays one change rather than two. The next session
raises it to the Dense step in the same pass that fixes the other 47; do not
treat it as a considered choice. Nothing is unreadable — every one is on a token — but Storage's
card titles are still capitals while the rest of the page is sentence case,
and that is visible in `c3b-after-storage-1440x900.png`.

**The next session's first job is those two files' type pass, after the
tracks merge.** It is the only thing standing between this page and "reads
as the same system" being true without a caveat.

Also not done, and deliberately:

- **AT-20's S3 three-block regroup** (Connection / Credentials / Verify with
  an eyebrow per group). It is a layout change inside `StorageSection`, which
  Track C owns. Ready to do the moment that file is free.
- **The eleven `StorageSection` branches were not driven by hand.** Review
  Risk 1. Tester mode cannot reach them: they need a real `workspace_storage`
  row. The token sweep is uniform across the file so an unreached branch is
  no more likely to be wrong than a reached one, but that is an argument, not
  a check. **This is the largest residual risk in the bundle.**

---

## 5. Traps hit

1. **🚨 THE DEV SERVER SERVED THE WRONG CHECKOUT, AND LOOKED FINE.**
   `preview_start` with the worktree's `wilson-dev` entry resolved its cwd to
   the **main checkout**, which is on a pre-overhaul branch with no `src/ui/`
   at all. The app booted, the page rendered, and every screenshot would have
   been of the wrong code. It was caught by probing for a file only this
   worktree has: `curl /src/ui/Table.jsx` returned `index.html` (Vite's SPA
   fallback) instead of a module. **Probe for a worktree-unique file before
   trusting anything you see.** The fix is a launch entry that passes the
   root: `npx vite <abs path> --port 5221 --strictPort`. Vite takes the root
   **positionally** — `--root` is an unknown option and exits 1.

2. **🚨 A STATE TEST THAT CHECKS A VALUE THE STYLESHEET DOES NOT READ PROVES
   NOTHING.** The kit's `Row` writes `data-selected={selected || undefined}`
   and `index.css` keys on the attribute's **presence**
   (`.ui-tr[data-selected]`). C3's extraction spells state
   `data-x={String(<expr>)}`, which renders `data-selected="false"` — present,
   so it paints. Passed straight to `<Row>`, **every roster row rendered as
   selected and deactivated at once**, and:
   - the source-scan guard passed (the spelling was right),
   - the seven render tests passed (they asserted the string `"false"`),
   - the full suite passed, the build passed,
   - and the page was visibly wrong in the first screenshot.

   Both spellings are correct for their own side; the defect is the two
   meeting. **Before passing a `data-*` to a kit component, grep `index.css`
   for whether that selector reads the value or the presence.**
   `adminTerminalCss.test.js` now fails the build on the combination, and the
   render tests assert absence rather than `"false"`.

3. **`git checkout -- <file>` reverts your uncommitted work along with the
   mutant.** Reverting a deliberate break in a file that also held an
   uncommitted fix silently undid the fix; the next run showed *four* failures
   where the mutant had caused three, which is the only reason it was noticed.
   C3 recorded the untracked-file half of this trap; this is the tracked half.
   **Commit before mutating, or re-apply after reverting and re-read.**

4. **Git Bash rewrites a leading-slash argument into an MSYS path.**
   `--pages /admin-terminal` became `c:/Program Files/Git/admin-terminal`.
   `scripts/screenshot.mjs` documents the `root` alias for a bare `/` and the
   same trap reaches every path argument. Prefix the command with
   `MSYS_NO_PATHCONV=1`.

5. **A quoted bash heredoc still mangles a Python script containing regex
   escapes**, in a new way from C3's: `\s` and `\{` inside a heredoc emit
   `SyntaxWarning` and the substitution silently fails to match, so the edit
   "succeeds" and changes nothing. C3's advice stands and is stronger than it
   looked: **write the script with the Write tool and run it by path.** Every
   edit in this session that used a heredoc had to be redone.

6. **A single grep lies, again, in a new shape.** `grep -c "<h1"` reported 1
   after the `<h1>` was deleted — the hit was the word `<h1>` inside the
   comment explaining the deletion. C3 recorded stale grep output; this is
   grep matching prose. **Read the matching line, not the count.**

7. **`grep -c $'\r'` under Git Bash reports 0 on a CRLF file.** The shell
   strips the CR from the quoted argument, so a line-ending check written that
   way says "all LF" about a file that is entirely CRLF. Use `od -c`.

8. **A guard derived from two sources can pass while checking nothing, and
   did, twice in ten minutes.** The presence-attribute guard's component list
   came from a regex that only matched `import X, { Y } from`, so it found
   neither `Button` (a plain default import) nor `Table`, and scanned an empty
   set. Both derivations now have an explicit "the scan is not empty" test
   beside them. **Every derived list needs a companion assertion that it is
   populated.**

9. **🚨 A SCRIPTED TOKEN SWEEP COLLAPSES DISTINCTIONS IT CANNOT SEE.** Two
   files took a find-and-replace map from light-ground literals to dark
   tokens, which is the right treatment for files another track owns — and it
   silently destroyed two deliberate one-step contrasts. `rgba(120,70,30,0.08)`
   on a row and `rgba(120,70,30,0.12)` on the panel inside it both became
   `paper-raised`: **1.00:1, no boundary at all**, on the panel that says what
   approving a change request will do. A dark brown ink and a pale orange fill
   both became the signal, which is 3.75:1 against itself. **After any sweep,
   list the source values that mapped to the same destination and look at each
   pair.** Neither defect is visible in a diff — both lines are correct on
   their own.

10. **A `:disabled` rule on a control does not reach children that set their
    own colour.** The Storage pickers' disabled state was written, tested for
    in the sheet, described in a comment — and reached nothing, because every
    string inside the button carried an inline `color`. The guard that catches
    inline-beats-class only looked at the `.at-*` element itself, not at its
    descendants. **A state on a container has to name the descendants it
    colours, or it is a declaration rather than a state.**

11. **A media query on the VIEWPORT, inside a container the viewport stopped
    driving.** The page is capped at the data measure, so the section pane
    saturates at about 1016px — while a `max-width: 1320px` rule kept widening
    the detail panel past that point. The result was that a window 1px wider
    than 1320 permanently cost the table 60px, and the roster was at its
    NARROWEST at the plan's own 1440x900 reference size. **Inside a capped
    container, measure the container, not the window.**

12. **The C: drive hit 0 bytes free mid-session**, and the first symptom was
    `sed: couldn't flush stdout: No space left on device` on a `docs/` read —
    not on a build. It cleared on its own within a couple of minutes (931G
    used, then 19G free) and nothing was damaged: the working tree was clean,
    `HEAD`'s tree was readable, and the last push and test run had both
    succeeded. **Check `df -h /c` before concluding a tool is broken**, and
    check `git status` and `git cat-file -e HEAD^{tree}` before assuming it
    is not. A build or an `npm install` landing in that window is the case to
    worry about; a read is not. `du -sh node_modules` on Windows takes longer
    than a 300s timeout, so do not reach for it to find out where the space
    went.

13. **GitHub was unreachable intermittently, again** — one push failed with
   `Failed to connect to github.com port 443 after ~21s` and succeeded on
   retry seconds later. Third session running. Plan §6.3's "retry on
   rejection" covers it.

---

## 6. Waiting on Audrey, and requests for other lanes

**Waiting on Audrey:** nothing blocking. The walkthrough
(`docs/walkthroughs/27_ui_admin_terminal.md`, part two) asks for two things
when convenient:

1. The **credentials pop-up's exit changed** and she should know: it now has
   an X and answers Escape, because Q17 ruled Escape-to-close in kit-wide.
   The two-step "Close without copying?" gate holds on all three exits, so
   nothing can lose a password in one action — but it is the one behaviour
   this bundle changed and it is on a show-once screen.
2. The **Users roster**, clicked through. It is where the one real defect was,
   and a second pair of eyes on "exactly one row marked at a time" is cheap.

C3's two questions are still open and still not blocking: the ADD PEOPLE menu
copy, and whether anything stopped responding.

**Kit requests (for whoever owns `src/ui/` next):**

- **`Tabs` inside `Toolbar` wraps.** `.ui-tabs` is `flex-wrap: wrap`, which
  R.A.B.B.I.T.'s eleven-tab strip needs and a two-tab toolbar must not have.
  Scoped locally here (`.at-section .ui-toolbar .ui-tabs`) exactly as C1 and
  C2 scoped `.ui-tab`'s height before F3 took it. **A `wrap={false}` prop on
  `Tabs`, or `nowrap` inside `.ui-toolbar`, retires three page-local copies.**
- **`Row` has no way to say "this state is false".** The presence/value split
  in §5 trap 2 is a kit-wide hazard: every `.ui-*[data-*]` selector that keys
  on presence will paint on a `String(false)`. Either the kit should key on
  `[data-x='true']` throughout, or `Row` should coerce. Twelve selectors are
  affected (`grep -o "\.ui-[a-z-]*\[data-[a-z-]*\]" src/index.css`).
- **`Badge` has no size below its default.** `.ui-badge` is 20px tall and the
  smallest `IconButton` is 28px, so a remove control mounted inside a badge —
  the department chips — grows the row unless the page gives it its own box,
  which this one now does. A `sm` badge, or an `xs` icon button, retires that.
- **`.ui-th-label` cannot ellipsise.** It is `display: inline` with
  `overflow: hidden; text-overflow: ellipsis`, and neither property applies to
  a non-replaced inline box. A clipped table header is therefore cut mid-word
  with no ellipsis, which reads as a rendering fault rather than as a narrow
  column — it is how round two's `RATE ACCESS` clip presented. `display:
  inline-block` plus `max-width: 100%`, or `min-width: 0` on a flex `.ui-th`.
- **A small `Select` loses its arrow clearance.**
  `.ui-input[data-size="sm"] { padding: 0 8px }` is (0,2,0) and
  `select.ui-input { padding-right: 28px }` is (0,1,1), so the shorthand wins
  and the chevron sits on the last character — about 1px of clearance on
  "All severities". Scoped locally here as
  `.at-section select.ui-input[data-size="sm"]`; the kit fix is
  `select.ui-input[data-size="sm"] { padding-right: 28px }`, or longhands in
  the sm rule.


**Not a request, a note for lane B:** `binUi.jsx` is untouched.

---

## 7. Next session's first steps

```
git fetch origin
git checkout -b ui/c3c-admin origin/feat/ui-overhaul
npm install --ignore-scripts && git checkout -- package-lock.json
```

Copy `.env.local` from `C:\Users\Audrey\Documents\My_Work\Dev_Work\wilson\WILSON\.env.local`
into `WILSON/` and add `VITE_DEV_AUTOLOGIN=tester` and `VITE_DEV_FIXTURES=1`.
Never commit it. **Add the `c3b-worktree` launch entry from §2 before
screenshotting anything** (§5 trap 1).

Read: this file; `docs/design/UI_OVERHAUL_PLAN.md` §0 C1–C9 and §3; the
Storage half of `docs/design/review/admin-terminal.md`;
`docs/sessions/HANDOFF_PROTOCOL.md`. You do **not** need to re-read C3's
hand-off — everything still live from it is repeated here.

### What is left, in order

1. **The type and case pass on `StorageSection.jsx` and
   `ChangeRequestsSection.jsx`.** 14 uppercase runs and 52 sites at 11px, one
   of them the `.at-prose` class in this page's own stylesheet. This is the
   only thing making "the Admin terminal reads as the same system" a statement
   with a caveat on it.

   🚨 **THIS IS NOT BLOCKED, AND AN EARLIER DRAFT OF THIS FILE SAID IT WAS.**
   The line read "once Track A and Track C have merged and those files are
   free", which would have idled a whole session waiting for a merge that is
   Audrey's to make. The real constraint on these two files is the one C3b
   worked under all day and is written in its own chip: **tokens and kit
   components only, no JSX restructuring.** A type and case pass is class
   swaps — `text-[11px]` to a scale step, `uppercase` and `tracking-wider` and
   `font-bold` off the roles that should not have them — which is squarely
   inside it. Plan §6.6 says the same thing from the other direction: touch
   these files mechanically and a later merge conflicts on lines, not on
   structure, which is the cheap kind.

   Measured 2026-09-13: `track-a-product` is 63 commits and
   `track-c-storage` 37 commits ahead of `feat/multi-user-v1`, and each
   carries about 39 insertions to its own one of these two files. So the
   conflicts are real but small and mechanical, which is exactly what §6.6
   budgets for.
2. **AT-20, the S3 card's three-block regroup — THIS one IS blocked.** It
   moves JSX, which is the thing Track C's chip withholds, and the file has 37
   commits of unmerged work sitting on it. Wait for the merge. Connection /
   Credentials /
   Verify, an eyebrow per group, a hairline above, 24px between groups
   against 8px within, one filled primary per region. **The visual-only
   reframe, not the original finding:** nothing is hidden and no control moves
   behind a click. Same file, same unblocking.
3. **Drive `StorageSection`'s eleven conditional branches by hand** with a
   real `workspace_storage` row (§4). The list is in the review's Risk 1.
4. **Retire the local `.at-section .ui-toolbar .ui-tabs` scope** when the kit
   takes the `wrap` prop (§6).
5. **Get a CI status recorded.** Two sessions have now owed one.

### Things already settled — do not re-derive them

- W9 is a no-op here: zero `window.confirm` in this directory, verified three
  times across two sessions. The in-flow confirms stay in-flow.
- There is no `fmtCurrency` in this directory.
- The third `ThLight` is in `src/admin/`, out of scope (Q14).
- No `ShortcutBar` (Q10). AT-23 is dead.
- AT-06 and AT-24 dissolved with the ground flip.
- The `display` ternaries in `AdminTerminalPage` stay. Five sections
  lazy-fetch behind a `loadedRef`; a remount refires the RPCs on every hop.

---

## 8. The adversarial review rounds

### Round one — two reviewers, one on state and behaviour, one on the design system

Both ran against `673b2dd` while the branch moved under them; the second
re-verified everything at `280c9d3` and said so, which is the right shape.

**The single most useful thing either found had already been found by looking
at the page.** The first reviewer independently reproduced the Enter
regression, including the mechanism — the kit `Input` blurs on Enter, and a
blurred field triggers no implicit submission — and reached it from the source
where this session reached it from the running app. Two routes, one answer, is
the strongest confirmation available.

Everything acted on, with the measurement that made it a defect:

| Finding | Measured | Fix |
|---|---|---|
| Storage's five warning banners, signal ink on signal tint | **3.75:1** at 11px | ink on the tint, 12.80:1 |
| The quota meter's alarm state | **2.21:1** on its own track, and the last two raw hexes in the thirteen files | `danger`, 8.52:1 |
| Change requests' approve/decline panel against its row | **1.00:1** | hairline + recessed ground |
| The Storage pickers' disabled state | never landed; inline colour on the children | classes, and the state named on the descendants |
| The overridden model row's caption | **4.18:1** over the highlight | second ink, 6.48:1 |
| The roster's columns with the panel open | Username and Joined clipped at **every** size | budget follows the longest real string |
| The panel's `1320px` breakpoint | a window 1px wider cost the table 60px permanently | one width, no breakpoint |
| Every toolbar | inset 24px from everything above and below it | the page's gutter, not two |
| `.at-search-input`, `.at-input-grow`, `.at-note-field` | inert; rendered at 204px, not 260 | element-qualified selectors |
| The `1180px` media query | set `.at-nav` to the width it already had | a real step |
| `data-tone="chip"` | read by nothing, in either stylesheet, for a whole commit | deleted, with the badge mounting made a rule |
| `MultiInviteDialog`'s own Escape listener | on top of `Dialog`'s, and it never checked which modal was topmost | deleted |
| The ADD PEOPLE 300ms guard | suppressed the next click after **any** dismissal | keyed on what closed it, not when |
| The credentials armed prompt | reachable from Escape and the X with no announcement | `role="status"` |
| 22 × `rounded-sm` | 4px, a radius §3.3 deleted | `rounded-control`, 3px |
| The fifth private `statusColor` map | and it had collapsed `open` and `changes_requested` onto one value | `StatusDot` tones |
| `38vh` | 266px at the 700 floor against 342 at 900 | px |

**The four guards that let all of it past**, each rewritten and each proved by
reintroducing the defect it names:

1. The C8 check read the stylesheet and **not the thirteen files**, which is
   exactly how two raw hexes survived in live code.
2. The pairing check ran CSS → JSX only, so an attribute whose rule was
   deleted passed as long as its class kept any other rule. 🚨 **The first
   rewrite was itself inert** — a name-based exemption list let the
   `data-tone="chip"` mutant straight back through, which is the R1-fix-is-an-
   inert-guard failure this lane has recorded before, caught here only because
   the mutant was actually run. It is value-based now: a literal value that no
   selector in either stylesheet accepts is dead.
3. An unparsed `style=` was treated as a clean one, so
   `style={c ? {…} : undefined}` re-admitted the entire inline-beats-class
   class of defect in one token.
4. The scan read **its own test file**, so the pairing check could be
   satisfied by an element in a test rather than in a component.

Four render tests were added for the nav, which had none. The mutation they
close — `data-active={String(!active)}` — keeps the source scan green, keeps
the class/attribute pair green, and highlights the six sections you are not on.

Two findings were recorded rather than fixed, both correctly scoped out:
**56 sites of body copy at the 11px Label step** in the two files Track A and
Track C own, and the residual kit requests in §6.

### Round two — it attacked round one's corrections in the running app, and broke four of the guards

Round two was told to verify by driving the page, not by reading, because
round one had already shown that reading the diff again finds nothing. It
built its own isolated snapshot of the tree on a separate port when it noticed
this session still committing under it, which is the right instinct and is why
its numbers are trustworthy.

It confirmed **fifteen** of round one's corrections as good, with measurements
— the re-inked Storage banners at 11.90:1, the quota meter's two states at
8.52 and 4.54, the overridden caption at 7.09, the search field rendering at
260px instead of 204, the page capping at 1288 and the table saturating at
1016, the menu reopening immediately after all four ordinary dismissals. That
list is as useful as the findings.

**🚨 The worst defect of the whole session was found here, in a fix.**

`CredentialsPopup`'s gate opened on **any** copy, including the username —
which is the top row, and the obvious first click. Copy the username, press
Escape once, and a show-once password was gone for good. The four gate tests
round one added all exercised the nothing-copied branch, so a real data-loss
path sat under four green tests, and `docs/walkthroughs/27_ui_admin_terminal.md`
told Audrey in as many words that "nothing can lose you a password in one
action". The gate is keyed to the password now, three tests cover the branch
the first four missed, and the walkthrough says what happened.

The other three HIGHs were all round-one corrections that did not reach:

| | |
|---|---|
| The toolbar gutter fix | missed `UsersSection`, the one section of seven whose root was not `.at-section` — and the one an admin lands on. Measured: contents at 348 against a title at 324. |
| The menu's mousedown flag | replaced a 300ms dead button with an **indefinite** one. A mousedown on a button is not a promise of a click on it; press-and-slide-off, the universal cancel gesture, left it armed through a section hop and back. Any click clears it now, verified on all five paths in the app. |
| The roster rebalance | fixed Joined and newly broke Role, the RATE ACCESS header and Status. |

**The roster could not be fixed by rebalancing at all**, and that is the
useful part. Measured against the longest real string in each column, the six
minimums sum to about 101 percent of the 752px the table gets with the panel
open. Two rounds of percentages moved the clip around. It declares a
`min-width` now and the kit's scroller takes over below it — verified at
1280 with the panel open: table 860, scroller 734, scrolls sideways, and no
cell clips including an injected `Deactivated`.

**All four of round one's new guards were evadable, and it proved each one by
reintroducing the defect and running the suite green.** Every evasion is now
caught, and two of the repairs needed a second attempt:

- The colour scan matched single quotes and whole tokens only, so `"#9a3412"`
  walked through — and so did `border: '1px solid #9a3412'`, the exact shape
  of the style object C3's own first review tripped over. 🚨 **The obvious
  repair, pairing quotes properly, was ALSO green against the same mutation**:
  these files are CRLF and full of apostrophes in prose, so a quote matcher
  mispairs immediately and every offset after it is fiction. It is a flat scan
  now, which is the honest check when no colour literal is legitimate at all.
- The orphan-value scan matched double-quoted literals only; `data-tone`
  returned as `='chip'` and `={'chip'}`.
- The unparsed-`style=` check sat **behind** the class filter, so an element
  with no `at-*` class was invisible — which is exactly the shape of the
  defect it guards, since the pickers were killed by inline colours on
  Tailwind-classed children. It runs first now. A partial revert (two of five
  spans) still slipped past the class-based check, so a countable invariant
  covers that: two pickers, two labels, two blurbs, two icons.
- The textarea test asserted that the form has no textarea — a fact about
  today's markup. Deleting the clause it is named for left it green.

Round two's MEDIUM and LOW findings were all taken: the disabled ink on an
active picker at 3.88:1, the quota meter's invisible trough at 1.00:1 against
its own card, the decide panel's 1.51:1 hairline (it takes the signal edge
now), and a three-column table skeleton standing in for a one-column card
list.

One kit request came out of it and is in §6: `.ui-th-label` is `display:
inline` with `overflow: hidden; text-overflow: ellipsis`, which does not apply
to a non-replaced inline box — so a clipped table header truncates with no
ellipsis, which is why the RATE ACCESS clip read as a rendering glitch rather
than as a narrow column.

---

## 9. Auto-memory: none

This worktree's memory folder is its own and empty, and the main repo's is
not this session's to edit. This file is the memory; every lesson is in §5.

---

## 10. Closing state

- Branch `ui/c3b-admin` pushed to origin, twelve commits.
- `feat/ui-overhaul` fast-forwarded to this branch's head.
- **vitest 2720 / 137 files**, green with and without `.env.local`.
  `npx vite build` clean but for the two pre-existing warnings.
- Both review rounds closed. Round one's corrections are in `a2ad448`; round
  two's are in `e4f2619` and `d12a74d`. Every evasion either round
  demonstrated has been re-run against the repaired guard and now fails.
- Screenshots re-taken after the last fix, not before it — the first set was
  captured six commits early and showed defects two of which the reviewers
  then found in it.
- No chip spawned; the controller sequences lane C.
- Dev server on 5221 stopped; Browser pane viewport reset.
- Last command: `git checkout --detach`.
