# RABBIT SQL — Session 1 scaffold marker

Session 2 populates:

- `schema.sql` — full Postgres DDL (workspaces, users, projects, documents,
  phases, assets, tasks, task_deps, task_assignments, activity_log,
  rate_card_entries, …)
- `seed.sql` — v0.1 single-user workspace seed
- `migrations/0001_init.sql` — identical copy of schema.sql as the first migration
- `migrations/0002_rate_card.sql` — added by Session 3 if needed (or folded into 0001)
- `README.md` — how to run the migrations against a Supabase project

Full DDL is embedded in `Claude_Work/RABBIT_v0.6_session2_prompt.md` §4.
