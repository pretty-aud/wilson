# RABBIT adapters — Session 1 scaffold marker

Session 2 populates this directory with:

- `index.js` — adapter selector + `RabbitAdapter` interface typedef
- `supabaseAdapter.js` — full CRUD via `@supabase/supabase-js`
- `localServerAdapter.js` — full CRUD via Express routes under `/api/rabbit/*`
- `googleDriveAdapter.js` — **partial** (read-only for v0.1; write path documented in post-build notes for v0.2)

All three implement a single `RabbitAdapter` interface so swapping is a config change.

See `Claude_Work/RABBIT_v0.6_session2_prompt.md` §6 and §7 for the full spec.
