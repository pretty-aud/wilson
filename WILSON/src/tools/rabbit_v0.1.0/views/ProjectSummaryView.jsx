// RABBIT v0.1.0 — ProjectSummaryView (Session 1 scaffold).
// Session 3 builds the high-level read-only dashboard (header card, phase strip,
// next up, at risk, budget snapshot, recent activity).
import React from 'react';
export default function ProjectSummaryView() { return null; }
