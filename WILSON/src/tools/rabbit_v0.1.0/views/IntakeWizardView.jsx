// RABBIT v0.1.0 — IntakeWizardView (Session 1 scaffold).
// Session 3 builds the full upload → classify → core-definer → preview → run → review → save flow.
import React from 'react';
export default function IntakeWizardView() { return null; }
