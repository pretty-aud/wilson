# RABBIT components — Session 1 scaffold marker

Session 3 populates this directory with:

- `RabbitHeader.jsx` — top bar inside PageShell
- `ProjectPicker.jsx` — dropdown of projects in current workspace
- `ViewTabs.jsx` — Summary | Assets | Timeline | Budget
- `IntakeUploader.jsx` — drag/drop + file list
- `DocKindClassifier.jsx` — auto-classify + manual override
- `CoreDefinerSelector.jsx` — is_core_definer flag per file
- `IntakeProgress.jsx` — chunk progress bar + partial results
- `AssetsTable.jsx` — inline-edit row table
- `AssetsGallery.jsx` — Notion-style card grid
- `AssetTypeTabs.jsx` — All | Characters | Locations | Props | VFX | …
- `AssetStatusWarningModal.jsx` — warns when approving asset w/ incomplete tasks
- `BudgetSummaryTab.jsx`
- `BudgetByPhaseTab.jsx`
- `BudgetByRoleTab.jsx`
- `BudgetByAssetTab.jsx`
- `BudgetCustomTab.jsx` — user-configurable pivot
- `CurrencyDisplay.jsx` — reads Settings → formats with Intl.NumberFormat

See `Claude_Work/RABBIT_v0.6_session3_prompt.md` §3 for the full spec.
