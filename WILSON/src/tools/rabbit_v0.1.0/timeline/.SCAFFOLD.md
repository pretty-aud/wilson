# RABBIT timeline — Session 1 scaffold marker

Session 3 populates this directory with:

- `TimelineShell.jsx` — locked-left tree + scrolling canvas layout
- `SvarGanttCanvas.jsx` — SVAR React Gantt wrapper
- `SummaryBand.jsx` — custom React/SVG band above the Gantt
- `ZoomControls.jsx` — Day | Week | Month | Quarter | Year
- `ganttTheme.js` — CSS-var theme matching WILSON palette
- `timelineSelectors.js` — project → Gantt rows/links transform

SVAR React Gantt (`wx-react-gantt`, MIT, React 19 compatible) is installed in Session 3.

See `Claude_Work/RABBIT_v0.6_session3_prompt.md` §9 for the full spec.
