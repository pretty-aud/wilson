// STUB: Future shared header/footer chrome wrapper for tools
// Will provide consistent navigation and branding across all tools
export default function ToolShell({ children }) {
  return <>{children}</>;
}
