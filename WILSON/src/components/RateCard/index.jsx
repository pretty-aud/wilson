// ============================================================
// Rate Card — barrel export
// ============================================================
// The Rate Card module is a workspace-level resource (not project-
// scoped) that maps role slugs to day/week/month rates. It feeds
// the RABBIT BudgetView and is reachable from the Home → Resources
// slide-out submenu, plus the cross-page nav strip on every page.

export { default } from './RateCardPage'
export { default as RateCardPage } from './RateCardPage'
